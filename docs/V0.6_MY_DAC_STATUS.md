# EQ Library v0.6.0 — My DAC current status

Status date: 2026-09-13

This document is the concise current-state companion to `docs/V0.6_MY_DAC_IMPLEMENTATION_PLAN.md`, `docs/V0.6_MY_DAC_APPROVED_DESIGN.md`, and the maintained project runbook. It records what is implemented, what is physically proven, what failed, and what remains blocked.

## Current branch and release state

- Repository: `weekssa/opra-eq-for-uapp`
- Branch: `v0.6-my-dac`
- Draft PR: #16
- Product version: `0.6.0`
- Public release remains `v0.5.0`; v0.6 is not published.
- Do not merge or publish without explicit project-owner authorization.

Current device roadmap: **TRN Black Pearl -> FiiO**. JCALLY is not part of the current/upcoming product UX. Firmware remains read-only; firmware/bootloader/cross-flash, raw-register consoles, USB identity mutation, cached-state restoration, and speculative persistence remain prohibited.

## Implemented v0.6 My DAC behavior

The approved My DAC core is implemented, including:

- supported-DAC recognition and session-sticky My DAC navigation;
- current/stale USB-session handling;
- Black Pearl native EQ readback, deterministic matching, response visualization, editor, Review -> Apply -> verified readback, Save DAC EQ, Change EQ, and Reset EQ to flat;
- capability-driven DEVICE presentation for firmware, DAC filter, gain mode, amp topology, microphone gain, balance, and playback/global level;
- a typed DEVICE transaction contract that requires a current expected session, takes a complete fresh baseline, writes only the requested control, verifies complete readback, rejects unrelated-state changes, and never invokes generic Save-to-Flash;
- local staging and explicit Current/New review before writes;
- no cached-state replay on reconnect and no write retry after USB-session replacement;
- no requested value presented as current before readback verification.

The product accent is the approved TOPPING-reference blue `#257BFF`, while semantic warning/error/success colors remain semantic.

## Completed Black Pearl physical qualification

### Read-only DEVICE state — PASS

Pinned exact source:

`d3d227de45751f6606b75637a2fd311249afa88d`

Pixel 9 / TRN Black Pearl validation passed for firmware `0.6`, `Fast-PC`, `HIGH`, `CLASS AB`, mic `0 dB`, centered balance, and playback/global raw `512` / `+2.00 dB`, including independent-controller agreement, repeated non-destructive reads, external-filter freshness, disconnect stale-state behavior, reconnect freshness, and final no-write comparison.

Authoritative record: `docs/BLACK_PEARL_V0.6_READ_ONLY_QUALIFICATION.md`.

### DAC reconstruction-filter write — PASS

Pinned exact behavior source:

`f7a3179d8124c70175e377c8673958e2c83d28a3`

That exact candidate passed Android CI #1342, CodeQL #1223, Catalog currentness #1512, Priority community coverage #999, Signed EQ Library Beta Candidate #1016, and the complete maintained Pixel 9 / Black Pearl physical checklist.

The pass covered Cancel-without-write, one targeted `Fast-PC -> FAST-LL` change, complete verified readback, unrelated-state preservation, independent-controller agreement, restoration to `Fast-PC`, reconnect freshness, and conservative no-persistence wording.

Authoritative record: `docs/BLACK_PEARL_V0.6_DAC_FILTER_WRITE_QUALIFICATION.md`.

## First consolidated remaining-controls candidate — FAILED / STOPPED

First exact behavior candidate:

`a3837779b5b732f3b388f80a86c0b14fc00e34c5`

The automated/signing gates passed, but the 2026-09-13 Pixel 9 / Black Pearl hands-on round surfaced real issues:

- amplifier topology did not satisfy the required one-Apply -> verified-readback flow; the owner reported needing several refresh/retry attempts before the requested state was observed;
- EQ Library correctly displayed readback mismatch rather than falsely reporting success;
- the normal playback/global-level `0.5 dB` product-step assumption did not round-trip reliably; a whole-dB `+3.00 dB` / raw `768` state was observed during the mismatch sequence;
- the owner restored the complete reference state before ending the interrupted round: `Fast-PC`, `HIGH`, `CLASS AB`, mic `0 dB`, centered balance, raw `512` / `+2.00 dB` / approximately `63%`.

No PASS is inferred for balance, mic gain, or gain mode from that incomplete session. The failed `a383777...` APK remains immutable evidence only and must not be reused as the replacement physical candidate.

Authoritative record: `docs/BLACK_PEARL_V0.6_DEVICE_CONTROLS_BATCH_QUALIFICATION.md`.

## Replacement consolidated remaining-controls candidate — READY FOR RETEST

Exact replacement **behavior candidate**:

`001b5ea5b2199784cfc8cf34941b018bf927caa1`

Replacement changes:

- normal My DAC playback changes now use conservative whole-dB product steps; lower-level 1/256 dB protocol precision and the already-qualified EQ/Flash behavior are unchanged;
- playback +/- buttons advance from the staged draft instead of repeatedly recalculating from the original current value;
- fractional normal DEVICE playback targets such as `+1.5 dB` are rejected before USB traffic;
- unqualified normal DEVICE writes now get a bounded same-session read-only settling/reverification window after the single write;
- settling never resends the write, never crosses a USB-session replacement, and never hides a read failure, unrelated-state mutation, or final mismatch;
- the already-qualified DAC-filter path retains its original immediate single-readback behavior;
- automated regression coverage explicitly exercises delayed target visibility, never-settled mismatch, session replacement during settling, and half-dB playback rejection before USB traffic.

Exact replacement candidate gates:

- Android CI #1374 — **PASS**
- CodeQL #1255 — **PASS**
- Catalog currentness CI #1564 — **PASS**
- Priority community coverage CI #1050 — **PASS**
- Signed EQ Library Beta Candidate #1048 / run `34791576746` — **PASS**
- signed artifact ID `10328123788`
- artifact ZIP SHA-256 `8441cbfb793fa93e73ac3a8453fe642523339ab258873e1fa843d368bd1a17e6`
- established signer certificate SHA-256 `65c1c1256dae3c49e3548f334c91f0ba991969e9be9e0b223ba4e253d2114747`

Immutable exact-candidate APK:

`https://raw.githubusercontent.com/weekssa/OPRA-EQ-for-UAPP/mobile-test-apk/candidates/EQ-Library-v0.6.0-beta-001b5ea.apk`

Signed APK SHA-256:

`90214ff585cb9e413264b9c776c14b8a642d124c18984ceefa7cbe0d90ea0de8`

Documentation-only commits after `001b5ea...` do not replace this exact behavior candidate.

## Replacement hands-on scope

The retest remains one consolidated session with small reversible changes:

1. Balance: Center -> `-1 dB` -> Center.
2. Microphone gain: `0 dB` -> `-1 dB` -> `0 dB`.
3. Amplifier topology: `CLASS AB` -> `CLASS H` -> `CLASS AB`.
4. Gain mode: `HIGH` -> `LOW` -> `HIGH`.
5. Playback/global level, last because it is level-sensitive: raw `512` / `+2.00 dB` -> raw `256` / `+1.00 dB` -> raw `512` / `+2.00 dB`.
6. One independent-controller read-only comparison after restoration; never use Save to Flash.
7. One disconnect/reconnect freshness check and final complete EQ Library read.

After each Apply, the owner must let EQ Library finish its automatic verification. A failure stops the round; do not manually Refresh or retry a failed write.

Current qualification state:

- DAC reconstruction filter write — **PHYSICALLY QUALIFIED / PASS**
- Balance write — **REPLACEMENT EXACT SIGNED CANDIDATE READY / PHYSICAL PENDING**
- Microphone gain write — **REPLACEMENT EXACT SIGNED CANDIDATE READY / PHYSICAL PENDING**
- Amplifier topology write — **FAILED ON FIRST BATCH; FIXED CANDIDATE READY / PHYSICAL RETEST PENDING**
- Gain mode write — **REPLACEMENT EXACT SIGNED CANDIDATE READY / PHYSICAL PENDING**
- Playback/global level write — **FAILED ON FIRST BATCH; WHOLE-DB FIXED CANDIDATE READY / PHYSICAL RETEST PENDING; LEVEL-SENSITIVE**

## Work continuation state

- **Last physical task:** first consolidated remaining-controls candidate stopped after topology/readback and playback-step issues; device restored to reference state.
- **Last software task:** implemented bounded read-only settling verification plus conservative whole-dB playback DEVICE UX and focused regression tests.
- **Replacement behavior checkpoint:** `001b5ea5b2199784cfc8cf34941b018bf927caa1`, fully green and signed.
- **Current physical blocker:** one consolidated Pixel 9 / TRN Black Pearl retest on exact `001b5ea...` candidate.
- **Product-decision blockers:** none for the approved scope.
- **FiiO blocker:** JA11 software work may continue independently, but physical validation remains unavailable until hardware is present.
- **Current main at latest reconciliation evidence:** `f0e6c16d37cbf23ad8e339d49e31251ee4e3f828`; re-check main before the final release candidate.
- **Resume note:** do not repeat Black Pearl read-only or DAC-filter qualification. Resume from the replacement consolidated remaining-controls checklist.

## Release blocker

Even if the replacement DEVICE round passes, v0.6 is not automatically ready to publish. Final closeout still requires behavior freeze, main reconciliation review, maintained documentation/changelog/release-note synchronization, an exact final signed release candidate, all required Android/security/catalog/signing gates, full Pixel 9 / Black Pearl final smoke on that exact APK, and explicit project-owner authorization before merge/publication.