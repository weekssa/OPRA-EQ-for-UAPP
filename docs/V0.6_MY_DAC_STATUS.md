# EQ Library v0.6.0 — My DAC current status

Status date: 2026-09-14

This is the concise current-state companion to the approved v0.6 design, implementation plan, protocol notes, `docs/V0.6_DEVICE_UX_POLISH_APPROVED.md`, and maintained project runbook.

## Release state

- Repository: `weekssa/opra-eq-for-uapp`
- Branch: `v0.6-my-dac`
- Draft PR: #16
- Product version: `0.6.0`
- Public release remains `v0.5.0`; v0.6 is not published.
- Do not merge or publish without explicit project-owner authorization.
- Device roadmap remains **TRN Black Pearl -> FiiO**. JCALLY is not current/upcoming product UX.

## Connected-DAC UX state

The owner-approved simplified My DAC model is implemented:

- one shared ViewModel-scoped supported-DAC session across My EQs, My DAC and EQ Library;
- automatic fresh EQ + DEVICE reads after connection/reconnection;
- automatic state refresh after successful Flash/reset/editor Apply/DEVICE writes;
- no second My DAC connection/read ceremony during normal navigation;
- no My DAC **Change EQ** chooser;
- Flash from **My EQs** or directly from a compatible **EQ Library** profile when exactly one supported DAC session is connected;
- no guessing when more than one supported DAC is connected;
- Black Pearl DEVICE is one concise current-value settings list with row-level editing and only short level-sensitive confirmations;
- Black Pearl UAC mode remains read-only with the approved manual UAC 1.0/2.0 startup instructions;
- FiiO JA11 uses the same My EQs / EQ Library ownership model rather than a second EQ chooser in My DAC.

The transaction layer still owns fresh baseline/session/readback protection, target-only writes, unrelated-state verification, no write retry, and truthful failure reporting.

## Owner-reviewed DEVICE polish

The first finished-UX signed candidate, behavior source `f32b9c3a81f77a8650f1b6f730e4a4c9171310ad`, passed the automated/signing gates and the owner then reviewed the DEVICE UI on Pixel 9 / TRN Black Pearl. That review identified percentage-volume, Default-EQ-target wording and visible Apply-feedback refinements.

Those refinements are implemented. Behavior-changing polish source begins at:

`0926a83eca55287dabbb7158d49a654d4f7957a6`

The original post-polish hands-on candidate was:

`cb4f08b08bfe7a9315e43d773e457fa7293c505a`

It remains the evidence pin for the already-passed DEVICE polish, My DAC EQ, direct Flash, DAC filter, Gain and UAC presentation checks. During the final disconnect/reconnect section, the owner confirmed that physical reattach reached **Detected · USB session not open** and required tapping **Connect**. That is a real UX regression against the approved same-session reconnect behavior, so `cb4f08b` is not accepted for the reconnect gate.

## Automatic reconnect fix

The reconnect defect was traced to supported USB transports correctly noticing physical reattach but only reopening automatically when the transport was already in a pending Connecting state. After a completed session had been detached, reattach therefore remained detected/disconnected until the user tapped Connect.

The fix is implemented at the shared DAC-session owner rather than as a Black Pearl-only special case:

- automatic reconnect is armed only after that exact supported DAC has connected successfully once during the current ViewModel/app-session lifetime;
- mere first-time detection still does **not** silently open a USB session;
- a later physical reattach of the same supported DAC automatically calls the normal connection path;
- retained Android USB permission is reused when available; otherwise the normal Android permission flow remains authoritative;
- Connecting/Error states never trigger an automatic retry loop;
- reconnect opens a session only; it never writes cached EQ or DEVICE state;
- existing Connected collectors still perform fresh EQ + DEVICE reads after the replacement session opens;
- the same policy is used for Black Pearl and FiiO so future supported DACs can adopt the same lifecycle contract without duplicating UX logic.

Behavior source for this fix:

`a3bc1740ed44892f4e6b78f9d0a359e2a87ef663`

Exact immutable signed APK:

`https://raw.githubusercontent.com/weekssa/OPRA-EQ-for-UAPP/mobile-test-apk/candidates/EQ-Library-v0.6.0-beta-a3bc174.apk`

APK SHA-256:

`3154748c72ad576a0b13b8adbdf549cc36eede6feef900dd68eecbc65bf1d64a`

Exact-source validation passed:

- Android CI #1430 / run `34824152036` — unit tests, lint, debug APK and release APK **PASS**;
- CodeQL #1311 / run `34824152031` — **PASS**;
- Catalog currentness CI #1652 / run `34824152041` — **PASS**;
- Priority community coverage CI #1138 / run `34824152058` — **PASS**;
- Signed EQ Library Beta Candidate #1105 / run `34824148366` — **PASS**;
- signed artifact ID `10338699838`;
- artifact ZIP digest `sha256:1f5b2fbe1fa07d335713a3f81259c7c305d7389a3bc02d82e2d89ee7a31bcfe9`;
- signer certificate SHA-256 `65c1c1256dae3c49e3548f334c91f0ba991969e9be9e0b223ba4e253d2114747`, verified against the repository-pinned release identity.

A focused unit regression now protects three lifecycle rules: initial detection never auto-opens, one successful session arms later physical reattach for automatic reconnect, and Connecting/Error states do not loop reconnect attempts.

## Focused post-polish Pixel 9 / Black Pearl evidence

Completed on signed `cb4f08b` before the reconnect defect was found:

- Section A owner-requested polish — **PASS, OWNER-REPORTED**: shared attach/auto-read, percentage-only Volume, verified decrease/restore feedback, Default EQ target wording, and confirmed absence of unsupported factory reset.
- My DAC EQ current-state presentation — **PASS, SCREENSHOT**: verified hardware state and response graph shown, with no Change EQ chooser.
- Direct Flash from My EQs plus automatic My DAC refresh — **PASS, OWNER-REPORTED + SCREENSHOT**: owner confirmed deliberate final Flash confirmation; following screenshot showed the matching My EQs profile, updated non-flat graph, and refreshed playback gain.
- Representative routine non-volume DEVICE change — **PASS, OWNER-REPORTED**: DAC filter changed to another value and restored with Applying/verified feedback.
- Representative level-sensitive DEVICE change — **PASS, OWNER-REPORTED**: Gain changed to the alternate value and restored with the concise warning and verified feedback.
- UAC presentation — **PASS, SCREENSHOT**: DEVICE showed detected **UAC 2.0**, the approved manual UAC 1.0 and UAC 2.0 instructions, and no in-app software-switch claim.
- Disconnect/session stickiness — **PASS, SCREENSHOT**: My DAC remained in navigation and retained DEVICE values were explicitly marked **Last read** / stale.
- Reconnect without a second Connect tap — **FAIL on `cb4f08b`, OWNER-REPORTED + SCREENSHOT**: physical reattach remained at **Detected · USB session not open** until the owner tapped Connect.
- After that manual Connect, fresh current EQ + DEVICE state was shown and no cached write was observed. This is useful evidence for read freshness after a session opens, but it does not satisfy the automatic-reconnect UX gate.

Only the reconnect-specific behavior now requires repeat physical validation on signed `a3bc174`. The earlier unaffected passed evidence remains attached to `cb4f08b` and is not relabeled.

## Reset-device UX and evidence gate

The owner approved a generic, capability-driven **Reset device** pattern for future supported DACs:

- main DEVICE page stays minimal: heading `Reset device`, one sentence, and `Reset to factory defaults`;
- confirmation may offer **Also reset EQ to flat** OFF by default only when the exact device supports an independently verified EQ reset;
- success is shown only after the complete reset is verified;
- the same shared UX may be used by Black Pearl, FiiO, or future DACs while each model keeps its own exact reset implementation and defaults.

This action is **not currently exposed for Black Pearl**. Research recorded in `docs/BLACK_PEARL_V0.6_FACTORY_DEFAULTS_RESEARCH.md` found no sufficiently established exact factory-reset command/default table. A third-party controller's convenience `Factory Reset` sequence conflicts with independent reports of shipped/initial Black Pearl gain/topology and does not establish every supported control. Therefore EQ Library does not guess factory defaults from that sequence or from the project's qualification baseline.

FiiO or future models receive the same reset UI only after their exact reset/default semantics are established.

## Completed Black Pearl evidence

- **Read-only DEVICE state — PASS**, pinned to `d3d227de45751f6606b75637a2fd311249afa88d`.
- **DAC reconstruction-filter write — PASS**, pinned to `f7a3179d8124c70175e377c8673958e2c83d28a3`.
- **Post-polish Section A — PASS (OWNER-REPORTED)** on exact signed `cb4f08b08bfe7a9315e43d773e457fa7293c505a`.
- **Post-polish My DAC EQ / Direct Flash / DAC filter / Gain / UAC presentation checks — PASS** with evidence categories recorded above on `cb4f08b`.
- **Disconnect stale/session-sticky presentation — PASS (SCREENSHOT)** on `cb4f08b`.
- **Automatic physical reattach — FAIL on `cb4f08b`**, fixed in software at `a3bc1740ed44892f4e6b78f9d0a359e2a87ef663`, pending focused physical retest.
- The owner previously reported the consolidated `7b693c8` remaining-control candidate working in normal use, including correct UAC-mode readout. These are **OWNER-REPORTED** observations and useful behavior provenance; they are not silently upgraded into screenshot or independent-controller evidence for every individual remaining control.

## Manual Black Pearl UAC behavior

The app detects the actual current UAC mode from standard USB AudioControl descriptors. No safe Black Pearl software/HID UAC switch command is established.

The approved helper text uses the established manual startup behavior:

- **UAC 1.0:** disconnect the Black Pearl, leave headphones connected, hold **+** and **−**, reconnect USB, then release after the DAC powers/enumerates;
- **UAC 2.0:** reconnect normally without holding the buttons;
- EQ Library automatically reads and displays the active mode from the replacement USB session.

The detected descriptor state remains authoritative for hardware/firmware revisions where button behavior differs.

## Current blocker / next milestone

Run one focused Pixel 9 / TRN Black Pearl reconnect-only regression on signed `a3bc174`: establish one successful connection in the current app session, unplug, verify stale/session-sticky state, reattach normally, **do not tap Connect or Refresh**, and verify that the app automatically opens the replacement session and freshly repopulates EQ + DEVICE state without any Applying/Flashing indication or cached-state push.

The hidden factory-reset capability is not part of this regression and must not be exercised or guessed.

## Final release gate

After the focused reconnect retest passes, v0.6 still requires behavior freeze, current-main reconciliation review, final changelog/README/release-note closeout, exact final signing/security/catalog gates, final Pixel 9 / Black Pearl release smoke if the release source changes, and explicit project-owner authorization before merge/publication.