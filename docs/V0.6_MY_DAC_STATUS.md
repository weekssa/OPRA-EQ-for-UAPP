# EQ Library v0.6.0 — My DAC current status

Status date: 2026-09-13

This document is the concise current-state companion to `docs/V0.6_MY_DAC_IMPLEMENTATION_PLAN.md`, `docs/V0.6_MY_DAC_APPROVED_DESIGN.md`, and the maintained project runbook. It records what is implemented, what is physically proven, and what remains blocked.

## Current branch

- Repository: `weekssa/opra-eq-for-uapp`
- Branch: `v0.6-my-dac`
- Draft PR: #16
- Product version: `0.6.0`
- Public release remains `v0.5.0`; v0.6 is not published.

Behavior checkpoint `15694b0034d4191ab2cac10b592f122587b9bcc7` passed Android CI #1238, CodeQL #1119, Catalog currentness CI #1350, and Priority community coverage CI #838.

Current `main` through `bc0de4101d204a9af614b96b1df5382ee5f7af19` was reconciled into `v0.6-my-dac` by merge `2863bba7262844bfe32857c09607c37eba8f4505`. Post-reconciliation head `6d9e997354b3e5678773da6f727428b335b12a0b` passed Android CI #1261, CodeQL #1142, Catalog currentness #1374, and Priority community coverage #861. Later work does not replace the eventual final exact release-candidate/signing gate.

Software checkpoint `45566224ee7da340b9399b789c8d61b8ce9a7754` passed Android CI #1338, CodeQL #1219, Catalog currentness #1505, and Priority community coverage #992. It removed JCALLY from active hardware runtime ownership while retaining only temporary compatibility plumbing above that boundary.

The exact Black Pearl **DAC reconstruction-filter write qualification candidate** is source `f7a3179d8124c70175e377c8673958e2c83d28a3`. That exact behavior head passed Android CI #1342, CodeQL #1223, Catalog currentness #1512, Priority community coverage #999, and Signed EQ Library Beta Candidate #1016. The same exact signed candidate passed the complete focused Pixel 9 / TRN Black Pearl DAC-filter write qualification on 2026-09-13.

## Current product direction

The project owner approved the next v0.6 build cycle on 2026-09-13 and authorized aggressive autonomous development.

Current device roadmap:

**TRN Black Pearl → FiiO**

JCALLY is no longer part of the active/current/upcoming product. Useful historical protocol and safety research may remain internally, but normal users must not see JCALLY as supported, pending, planned, experimental, or coming soon. A connected JCALLY device must follow the normal unsupported/unrecognized-device path.

The approved visual direction replaces the prior purple identity with the TOPPING-reference blue `#257BFF` (`RGB 37, 123, 255`) while preserving semantic error, warning, success, and disabled colors.

The approved Black Pearl objective is full normal day-to-day DAC management as far as independently established and safely qualified. Software work may implement the complete normal-control framework, but each new physical write graduates independently. Qualification order remains:

`DAC filter → balance → microphone gain → amp topology → gain mode → playback volume`

Firmware remains read-only. Firmware/bootloader/cross-flash, raw-register consoles, USB identity mutation, cached-state restoration, and speculative persistence remain prohibited.

## Implemented v0.6 My DAC behavior

### Session / navigation

- supported-DAC recognition is separate from USB connection state;
- My DAC appears immediately beside My EQs after a supported DAC is recognized;
- My DAC remains available for the current app session after disconnect and shows stale/Last-read state instead of making navigation jump;
- supported USB attach uses Android's normal permission/default-app flow;
- an already-open app uses the nonblocking **Open My DAC** prompt;
- connected-DAC context does not silently change the separate global active output.

### EQ state and editing

- read-only native Black Pearl hardware EQ snapshots;
- deterministic native fingerprints and Flat / Exact / ambiguous Exact / Unknown matching;
- Modified lineage only after a verified in-app edit that began from a known saved representation;
- reconnect or outside native change invalidates Modified lineage back to Unknown;
- compact and editor response graphs with tap-only band selection;
- exact filter type/frequency/gain/Q editing and All Bands view;
- deterministic complete-response headroom assessment;
- Review before Apply;
- fresh-baseline checks, safe gain ordering, write, readback, and verification;
- Save DAC EQ as a Personal EQ with capture provenance and optional real headphone association;
- Change EQ from the Black Pearl-specific My EQs collection with Exact / Optimized / Not suitable status;
- Reset EQ to flat through the already-qualified transaction.

### DEVICE architecture

- `DacControlRepository` remains separate from `HardwareEqRepository`;
- Compose receives typed capability/presentation state and contains no USB request/register constants;
- Black Pearl DEVICE state covers firmware, DAC filter, gain mode, amp topology, mic gain, balance, and playback/global gain;
- all fields must be read successfully from one current session or the complete snapshot fails;
- stale/replacement USB sessions are never promoted to current merely because the device reconnects;
- the generic Black Pearl write transaction requires a current expected session, takes a complete fresh baseline, writes only the requested control, performs complete readback, verifies the exact requested result, rejects unrelated-state changes, and never invokes the generic flash/save command;
- product exposure is admitted one independently qualified control at a time.

### Current next-cycle software work

Completed checkpoints include:

- `63a1e35971c76977f4e0f1c72525c62137be052f` — centralized TOPPING-style blue product accent in the Material theme.
- `96f3b866ffef3952e88006f06e0566142ae9fe32` — Black Pearl DEVICE write core for filter, gain mode, amp topology, balance, mic gain, and playback/global gain, including automated codec/repository regression coverage.
- `39e5608f56974f9320f849b05616ee06dabb872f` — removed JCALLY from the current supported USB recognition registry.
- `45566224ee7da340b9399b789c8d61b8ce9a7754` — removed JCALLY from active hardware runtime construction/ownership; fully green across Android CI, CodeQL, catalog currentness, and priority-community coverage.
- `a030e6d7fe47fe9d418ef491725ea5719523b2f7` — wired the first Black Pearl DEVICE write qualification candidate, **DAC filter**, end-to-end through ViewModel and Compose.
- `4a62ad81dfb14701f928bc3e40b45adc863ee54e` — hardened transaction presentation so an in-flight Black Pearl DEVICE read/write remains visibly busy until completion and Refresh cannot race a DEVICE write.
- `f7a3179d8124c70175e377c8673958e2c83d28a3` — added regression coverage for the busy-state/current-session projection and became the exact signed DAC-filter physical candidate.
- `c3d6d06e52b353342c2736de801dd1f9bf3babb7` — documentation-only closeout recording the DAC-filter physical qualification PASS.

## Exact Black Pearl read-only hardware candidate

The completed read-only physical qualification remains pinned to:

- source commit: `d3d227de45751f6606b75637a2fd311249afa88d`
- app version: `0.6.0`
- immutable APK: `https://raw.githubusercontent.com/weekssa/OPRA-EQ-for-UAPP/mobile-test-apk/candidates/EQ-Library-v0.6.0-beta-d3d227d.apk`
- APK SHA-256: `ea8235ee1ba2828873b488054b25d40c917b844c2d76a813bf9e038cf31c8ee5`
- signer certificate SHA-256: `65c1c1256dae3c49e3548f334c91f0ba991969e9be9e0b223ba4e253d2114747`
- Android CI #1226 — PASS
- CodeQL #1107 — PASS
- Catalog currentness #1331 — PASS
- Priority community coverage #819 — PASS
- Signed EQ Library Beta Candidate #901 — PASS

## Black Pearl read-only qualification result

**PASS — 2026-09-13.** The pinned candidate passed the complete maintained read-only DEVICE qualification on the project owner's Pixel 9 / TRN Black Pearl.

Verified behavior includes in-app connected recognition; one complete same-session seven-field read; independent agreement for firmware `0.6`, `Fast-PC`, `HIGH`, `CLASS AB`, mic `0 dB`, centered balance, and playback/global raw `512` / `+2.00 dB`; controller `Volume 63%` reconciliation; three stable non-destructive reads; external-filter fresh-read proof and restoration; disconnect stale-state behavior; reconnect freshness behavior; and a final independent no-write comparison with no unexpected persistence/state change.

## DAC-filter write qualification result

**PASS — 2026-09-13.** Exact source `f7a3179d8124c70175e377c8673958e2c83d28a3` passed the maintained `docs/BLACK_PEARL_V0.6_DAC_FILTER_WRITE_QUALIFICATION.md` checklist on Pixel 9 / TRN Black Pearl.

The physical pass covered:

- exact-candidate fresh baseline read;
- explicit `Current: Fast-PC -> New: FAST-LL` review;
- Cancel followed by a fresh read proving no write;
- one targeted `FAST-LL` Apply with verified readback and unrelated-state preservation;
- independent-controller semantic confirmation of `FAST-LL` with no Save-to-Flash action;
- restoration to `Fast-PC` through EQ Library with verified readback;
- reconnect freshness proving retained old-session state does not become current automatically;
- persistence wording remaining conservative: no claim that the transient DEVICE write is saved to flash or power-cycle persistent.

Deliberate mid-write USB failure injection was **not exercised on hardware** because exact-candidate automated coverage already exercises stale generation, session replacement, transfer failure, readback mismatch, invalid values, unrelated-state-change rejection, and busy-state/current-session projection.

This PASS qualifies only the DAC reconstruction-filter write. It does not qualify any later Black Pearl DEVICE control.

## New Black Pearl DEVICE write qualification state

Software implementation follows the strict model:

`fresh read → choose → review → targeted write → complete readback → verify requested value → verify unrelated state`

Current qualification state:

- DAC reconstruction filter write — **PHYSICALLY QUALIFIED / PASS**
- Balance write — **software transaction core implemented / next product candidate / physical qualification pending**
- Microphone gain write — **software transaction core implemented / product write hidden / physical qualification pending**
- Amplifier topology write — **software transaction core implemented / product write hidden / physical qualification pending**
- Gain mode write — **software transaction core implemented / product write hidden / physical qualification pending**
- Playback/global volume management — **software transaction core implemented / product write hidden / physical qualification pending; level-sensitive**

No DEVICE control may claim success before readback verification. USB session replacement invalidates a transaction; there is no silent retry or cached-state restore. Arbitrary DEVICE persistence/Save-to-Flash remains intentionally outside these transactions until independently qualified.

## WORK CONTINUATION STATE

- **Current branch:** `v0.6-my-dac`
- **Qualified DAC-filter behavior candidate:** `f7a3179d8124c70175e377c8673958e2c83d28a3`.
- **DAC-filter candidate gates:** Android CI #1342, CodeQL #1223, Catalog currentness #1512, Priority community coverage #999, and Signed EQ Library Beta Candidate #1016 all PASS.
- **DAC-filter physical gate:** PASS on 2026-09-13; authoritative record `docs/BLACK_PEARL_V0.6_DAC_FILTER_WRITE_QUALIFICATION.md`.
- **Current main at latest reconciliation evidence:** `bc0de4101d204a9af614b96b1df5382ee5f7af19`; re-check before final release candidate.
- **Last completed task:** closed the first Black Pearl write qualification with a verified DAC-filter PASS.
- **Exact next task:** admit **balance** as the next Black Pearl DEVICE product-write candidate, wire its deliberate local selection/review/apply UX using the already-tested typed transaction core, add focused regression coverage, validate the new exact software head, then pin one signed candidate before asking the project owner for the next physical test.
- **Tests passed:** exact DAC-filter candidate tests cover Android unit/lint/debug/release build, fresh baseline, stale generation, session replacement, transfer failure, readback mismatch, unrelated-state change, invalid values, signed balance codec behavior, representable playback-gain readback, and busy-state/current-session projection.
- **Tests failed:** none recorded for the qualified DAC-filter candidate.
- **Tests still pending:** balance candidate software/UI validation and physical qualification; later per-control qualification; final release-candidate full smoke; final release documentation/signing/publication gates.
- **Decisions made:** continue v0.6; product accent `#257BFF`; roadmap Black Pearl → FiiO; JCALLY absent from current UX/support; writes graduate one at a time; DAC filter is qualified; balance is next; no generic DEVICE Save-to-Flash.
- **Physical testing blocker:** none until the balance candidate reaches a new exact signed APK. FiiO hardware remains unavailable.
- **Product-decision blockers:** none for the approved scope.
- **Working tree:** changes are committed directly through GitHub; no uncommitted local working tree is relied upon.
- **Resume note:** do not repeat read-only or DAC-filter qualification. Continue directly with the balance candidate software work.

## Release blocker

See `docs/V0.6_RELEASE_CHECKLIST.md` and `docs/V0.6_BLACK_PEARL_FINAL_HANDS_ON_CHECKLIST.md` for controlled closeout. v0.6 must not merge or publish until behavior-changing work is frozen, all intended Black Pearl DEVICE writes have their own exact qualification, the final signed candidate passes required Android/security/catalog/signing gates and Pixel 9 / Black Pearl full smoke, maintained docs/changelog/release notes are synchronized, and the project owner explicitly authorizes publication.
