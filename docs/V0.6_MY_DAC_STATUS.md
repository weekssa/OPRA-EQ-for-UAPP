# EQ Library v0.6.0 — My DAC current status

## 2026-09-25 owner-reported FiiO JA11 Flash verification failure

The owner supplied a physical UI sequence showing a connected JA11 Flash attempt that failed on
global EQ gain readback after an Optimized 9-to-5-band conversion. A connected follow-up frame
shows the target bands with `-3.80 dB` global gain, while the Jaytiss source plan carries
`-3.90 dB`; a later reconnect frame shows flat bands and `0.00 dB`. This is negative physical
evidence, not a qualification result. Exact APK/source provenance, firmware/PID, raw protocol
bytes, operation phase, and restoration status remain unknown. JA11 remains implemented but
hardware-validation pending; no codec/tolerance change or public support claim follows from these
screenshots.

## 2026-09-25 JA11 correction — SIGNED SOFTWARE CANDIDATE READY / HARDWARE PENDING

The evidence-backed Save/reconnect correction is merged at exact source
`609911e2e51a254fc6f45b87fbdf4106c0049740` and has a complete signed candidate tuple:

- Exact APK: `EQ-Library-v0.7.0-beta-609911e.apk`.
- APK SHA-256: `583ff7014fc3c0977b6679cd8bf56d3a4f615411a629fa6014bab088ece082ef`.
- Signer certificate SHA-256: `65c1c1256dae3c49e3548f334c91f0ba991969e9be9e0b223ba4e253d2114747`; `CN=OPRA EQ for UAPP, O=weekssa`, RSA 4096.
- R8 mapping SHA-256: `9f9d0e28271b4cfefdc9e71c6416061bd4a99e38341ab72290b159825de6e6ec`.
- Signed-beta [run #1362](https://github.com/weekssa/OPRA-EQ-for-UAPP/actions/runs/36165218849): **PASS**; artifact ZIP SHA-256 `1b124c63cb799a5a069384d48a9477e32078167a509ccd8872b69693880988cd`.
- Android CI `36163197805`, CodeQL `36163197784`, priority coverage `36163197775`, and catalog currentness `36163197794`: **PASS**.

This is software, artifact, and emulator evidence only. JA11 remains hardware-validation pending
until the owner completes the exact physical checklist and restoration gate. No public support or
final release claim follows from this candidate.

## 2026-09-25 cross-DAC integration checkpoint

The live integrated source `a32770e522451ee1626cbd0dfd9e918c62a4faa1` preserves the
validated Black Pearl canonical Favorite behavior and the newer EW300/shared-DAC changes
from the release-candidate integration. Android CI, CodeQL, catalog-currentness, and
priority-community evidence recorded for the integrated source remain automated software
evidence only; no new Black Pearl or EW300 hardware session is inferred and this record does
not qualify FiiO JA11 hardware.

## 2026-09-24 Black Pearl legacy-library Favorites regression — OWNER-REPORTED PASS

The project owner confirmed that the TRN Black Pearl testing now passes, including the
previously failing Favorites action for the maintained legacy `Rtings/AutoEQ` library rows.
The exact signed candidate used for this regression is recorded below:

- Source commit: `505182edc1e7845ff342a2c28196b3b928ddd2f1` (merged PR #35).
- Exact candidate APK: `https://raw.githubusercontent.com/weekssa/OPRA-EQ-for-UAPP/mobile-test-apk/candidates/EQ-Library-v0.7.0-beta-505182e.apk`
- App version: `0.7.0` / version code `7`.
- APK SHA-256: `0237c1514f03a6b2a6987e35ccb2f3c6512a148aadfd2f7a232ca09742cf79ee`.
- Signer certificate SHA-256: `65c1c1256dae3c49e3548f334c91f0ba991969e9be9e0b223ba4e253d2114747`.
- Trusted signed-beta workflow: [run #1359](https://github.com/weekssa/OPRA-EQ-for-UAPP/actions/runs/36093590432) — **PASS**.
- Test-plan reference: PR #35 exact-source legacy Favorites regression scope.
- Evidence category: **OWNER-REPORTED**.

This closes the Black Pearl hands-on regression for the library Favorites issue. It does not
change the separately maintained Black Pearl protocol-qualification boundaries, establish new
factory-default semantics, or qualify FiiO JA11 hardware. The handset model/fingerprint was not
provided with this result, so this record intentionally remains owner-reported rather than a new
device-protocol qualification pin.
## 2026-09-15 v0.6.0 public release — COMPLETE

v0.6.0 is publicly released from merge commit `e5ffa5d00862edc3b79bf52e1508db5244845e94` via [Signed GitHub Release workflow run #35008912862](https://github.com/weekssa/OPRA-EQ-for-UAPP/actions/runs/35008912862).

- Public release: [EQ Library v0.6.0](https://github.com/weekssa/OPRA-EQ-for-UAPP/releases/tag/v0.6.0)
- Public APK: `EQ-Library-v0.6.0.apk`
- APK SHA-256: `93b5250d7f32b068f24702c9fbfacb697e7921100250ee0652981100e29adb62`
- Signing certificate: `65c1c1256dae3c49e3548f334c91f0ba991969e9be9e0b223ba4e253d2114747`
- Behavior evidence: exact signed candidate `eef5633e18a4ac311f110493e29633bf382675e3`, with owner-reported Pixel 9 PASS for the stable DEVICE header, shared bottom feedback, and Restore-defaults flow.
- FiiO JA11 physical qualification remains deferred and is not a blocker for this release.
- README, CHANGELOG, curated release notes, and the public release checklist have been updated with the final provenance.

## 2026-09-15 final Pixel 9 UX smoke — OWNER-REPORTED PASS


The owner installed the exact signed operation-feedback candidate `eef5633e18a4ac311f110493e29633bf382675e3` and reported **SUCCESS**.

This closes the focused Black Pearl/UI validation gate for:

- routine DEVICE setting changes using the stable header plus shared bottom Applying/Verified feedback;
- Restore-defaults progress and completion without inline layout movement;
- the approved Black Pearl flow with FiiO JA11 explicitly deferred.

The exact candidate remains:

`https://raw.githubusercontent.com/weekssa/OPRA-EQ-for-UAPP/mobile-test-apk/candidates/EQ-Library-v0.6.0-beta-eef5633.apk`

APK SHA-256: `4eee1f491971bd49ec72d10033b83a548c9b970c002befe79cc944f268a4cb59`.

The v0.6 implementation and focused physical/UI gates are complete. PR #16 was merged and v0.6.0 was published with explicit owner authorization.


## 2026-09-15 operation-feedback UX candidate — AUTOMATED PASS

The approved DEVICE feedback consistency pass is implemented on exact source `eef5633e18a4ac311f110493e29633bf382675e3`.

- DEVICE keeps a fixed connection summary and Refresh action; transient Applying/Verified/Error text no longer changes the settings layout.
- Black Pearl routine writes, reconnect verification, verified readback, failures, and Restore-default progress use the shared bottom Snackbar lifecycle.
- The operation lifecycle is modeled independently of protocol details so future capability-driven hardware adapters can reuse it.
- Unit tests, lint, debug/release assembly, CodeQL, catalog currentness, and priority-community coverage passed.
- Signed EQ Library Beta Candidate #1229 passed and published the exact test APK:
  `https://raw.githubusercontent.com/weekssa/OPRA-EQ-for-UAPP/mobile-test-apk/candidates/EQ-Library-v0.6.0-beta-eef5633.apk`
- APK SHA-256: `4eee1f491971bd49ec72d10033b83a548c9b970c002befe79cc944f268a4cb59`.
- The pinned signing certificate remains `65c1c1256dae3c49e3548f334c91f0ba991969e9be9e0b223ba4e253d2114747`.
- FiiO JA11 physical qualification is explicitly deferred and is not a blocker for this Black Pearl/UI release closeout.

The owner Pixel 9 smoke validation passed. Merge, release, and publication are complete; FiiO JA11 physical qualification remains the only deferred hardware gate.


## 2026-09-15 corrective candidate — PASS

The project owner completed the focused Pixel 9 / TRN Black Pearl retest on exact signed source `eb1980076009001b5216ffbb531de8a28a4780eb` and reported **SUCCESS**.

The retest confirmed:

- Restore defaults completed normally on the first attempt and reached **50% / FAST-LL / HIGH / CLASS AB / Centered / microphone 0 dB**.
- The optional EQ-flat behavior and saved My EQs remained protected as specified.
- Reconnect persistence remained correct.
- The revised My EQs managed-headphone detail and General EQs pages were materially less cramped, with the revised action/help hierarchy.

Evidence category: **OWNER-REPORTED**.

Exact signed APK: https://raw.githubusercontent.com/weekssa/OPRA-EQ-for-UAPP/mobile-test-apk/candidates/EQ-Library-v0.6.0-beta-eb19800.apk

APK SHA-256: `dabf4bcdddf69853b09793f5a94bec0a3af7efb430f1cdfe26ffc35a93b783ad`

The exact candidate passed Android CI #1538, CodeQL #1420, Catalog currentness CI #1826, Priority community coverage CI #1311, and Signed EQ Library Beta Candidate #1213. The signing workflow verified the pinned certificate `65c1c1256dae3c49e3548f334c91f0ba991969e9be9e0b223ba4e253d2114747`.

This PASS closes the corrective Black Pearl physical gate. It does not establish TRN factory-default semantics and does not qualify FiiO JA11 hardware behavior. Historical pre-publication note: this candidate passed its corrective physical gate; PR #16 was later merged and v0.6.0 published.


Status: **CORRECTIVE BETA — corrective physical retest PASS; final provenance and release authorization remain.**

## 2026-09-15 follow-up — failed candidate and corrective beta

Exact owner-tested source `431cbfa58eebe8c29fe8624a506106f728b613cc` failed the latest physical review.
APK SHA-256: `fbf7a30731c3159e592104d283953f7427357370158e7c6cc772c1a362bbf375`.
Evidence category: **OWNER-REPORTED**, with screenshot details transcribed in the owner's handoff (original images are not in this checkout).

- Restore defaults stopped with a verification error and left Volume at 0%; a later manual attempt succeeded.
- My EQs managed-headphone detail and General EQs used too much screen space before actual EQ content.
- This is **FAIL**, not a pass for premium items 4–7. The older exact `b0340842` restore and `b952aed8` premium items 1–3 observations remain historical evidence, not qualification of this correction.

Code inspection established a screen-state race: a newly issued step could be judged against the previous write's snapshot before the combined StateFlow delivered the new write state. The safety-volume write and final-volume write also share a control ID. Per-write generation tracking now requires completion of the exact newly issued cycle, including verified no-ops; stale emissions wait without sending another command. The restore is pinned to its starting USB session and final success checks every requested DEVICE value.

The correction changes restore orchestration and requires a focused signed-candidate physical retest. It does not add protocol commands, delays, write retries, or weaken readback/persistence checks.

**Genuine failure policy:** stop on an actual failed/unverified operation. Do not automatically raise volume from the safety floor while gain/topology, persistence, or the USB session is uncertain. Compensation is not justified by a verified volume opcode alone: that would raise listening level in an unverified mixed state. The error explains that volume may remain at 0% and directs the user to refresh DEVICE, then deliberately adjust Volume. The false-abort race is corrected so a healthy restore can reach its requested 50% normally.

Presentation: compact title/back row; neutral textual Connected state; notification preference and headphone removal in Headphone options; existing Manage presets and profile callbacks retained; profile actions wrap at large fonts; General EQ help is on demand; Save/Hide only appear with a selection; Select all/none stay discoverable; headers and content share scrolling so enlarged text cannot consume a fixed content viewport. Material touch targets remain at least 48dp.

Software regressions include delayed pre-write screen emissions across all seven steps, verified no-ops, distinct safety/final playback cycles, refusal of a different completed cycle, and a real transfer failure after the safety-volume write with no automatic retry or compensating volume increase.

Current gate: new exact-source Android CI, CodeQL, catalog currentness, priority community coverage, and signed-beta provenance, then focused Pixel 9 testing. The corrective physical retest PASS is recorded above. Historical pre-publication note: PR #16 was subsequently merged and v0.6.0 published.

## Historical record (exact evidence pins retained)

Status date: 2026-09-14

This is the concise current-state companion to the approved v0.6 design, implementation plan, protocol notes, `docs/V0.6_DEVICE_UX_POLISH_APPROVED.md`, `docs/V0.6_LIBRARY_OWNERSHIP_AND_RECOVERY.md`, and maintained project runbook.

## Release state

- Repository: `weekssa/opra-eq-for-uapp`
- Branch: `v0.6-my-dac`
- Merged PR: #16
- Product version: `0.6.0`
- Public release is `v0.6.0` (tag `v0.6.0`).
- Merge and publication completed with explicit project-owner authorization.
- Device roadmap remains **TRN Black Pearl -> FiiO**. JCALLY is not current/upcoming product UX.

## Connected-DAC UX state

The owner-approved simplified My DAC model is implemented:

- one shared ViewModel-scoped supported-DAC session across My EQs, My DAC and EQ Library;
- automatic fresh EQ + DEVICE reads after connection/reconnection;
- automatic state refresh after successful Flash/reset/editor Apply/DEVICE writes;
- session-sticky My DAC navigation after disconnect;
- automatic physical reattach after one successful connection in the current app/ViewModel lifetime;
- no second My DAC connection/read ceremony during normal navigation;
- no My DAC **Change EQ** chooser;
- Flash from **My EQs** or directly from a compatible **EQ Library** profile when exactly one supported DAC session is connected;
- no guessing when more than one supported DAC is connected;
- Black Pearl DEVICE is one concise current-value settings list with row-level editing and only short level-sensitive confirmations;
- Black Pearl ordinary playback is shown as percentage-first **Volume**;
- Black Pearl UAC mode remains read-only with the approved manual UAC 1.0/2.0 startup instructions;
- FiiO JA11 uses the same shared session/status architecture while retaining its own established native semantics.

The transaction layer still owns fresh baseline/session/readback protection, target-only writes, unrelated-state verification, no write retry, and truthful failure reporting. Reconnect opens and reads a fresh replacement session; it does not push cached EQ or DEVICE values back to hardware.

## Device-agnostic My EQs / recovery refactor

The later project-owner library decision is implemented and documented in `docs/V0.6_LIBRARY_OWNERSHIP_AND_RECOVERY.md`:

- My EQs ownership and profile selection are global/device-agnostic;
- active output changes compatibility, derived representation/currentness, Export and Flash behavior only;
- Favorites, Personal EQs, DAC captures, and General EQs remain the same local items across target changes;
- saving/adding to My EQs does not automatically Export or Flash;
- DAC capture uses the DAC only as provenance and becomes a normal Personal EQ;
- changing the active output no longer closes the open managed-headphone detail;
- persistent **Needs attention** derives unresolved exact app-owned artifacts from export ownership rather than crawling arbitrary storage;
- strict recovery preserves supported PEQ values and original-file provenance, while malformed/unsupported artifacts remain visible but not falsely recoverable;
- exact app-owned SAF deletion boundaries are preserved.

Legacy per-output tables and `outputId` repository signatures may remain temporarily for migration/source compatibility, but they no longer decide current My EQs visibility or identity.

## Exact Black Pearl v0.6 physical evidence

### Main post-polish candidate

Exact signed candidate:

`cb4f08b08bfe7a9315e43d773e457fa7293c505a`

This candidate remains the evidence pin for the already-passed finished-UX checks:

- owner-requested DEVICE polish — **PASS, OWNER-REPORTED**;
- My DAC EQ current-state presentation / response graph / no Change EQ chooser — **PASS, SCREENSHOT**;
- direct Flash from My EQs plus automatic My DAC refresh — **PASS, OWNER-REPORTED + SCREENSHOT**;
- DAC-filter change/restore — **PASS, OWNER-REPORTED**;
- Gain change/restore — **PASS, OWNER-REPORTED**;
- UAC 2.0 detection/manual helper — **PASS, SCREENSHOT**;
- disconnect retains My DAC and marks retained values **Last read** / stale — **PASS, SCREENSHOT**.

The final reconnect section on `cb4f08b` exposed one real defect: physical reattach stopped at **Detected · USB session not open** until the owner tapped Connect. That candidate therefore failed the automatic-reattach gate even though the earlier unaffected checks remain valid evidence.

### Automatic reconnect fix — PASS

Behavior source and exact signed physical retest pin:

`a3bc1740ed44892f4e6b78f9d0a359e2a87ef663`

Immutable signed APK:

`https://raw.githubusercontent.com/weekssa/OPRA-EQ-for-UAPP/mobile-test-apk/candidates/EQ-Library-v0.6.0-beta-a3bc174.apk`

APK SHA-256:

`3154748c72ad576a0b13b8adbdf549cc36eede6feef900dd68eecbc65bf1d64a`

On 2026-09-14 the project owner completed the focused Pixel 9 / TRN Black Pearl reconnect-only retest on this exact signed candidate and reported **SUCCESS**. Evidence category: **OWNER-REPORTED**.

The passed sequence established one successful current-app-session connection, unplugged the Black Pearl, preserved session-sticky My DAC with stale/Last-read state, reattached normally without an app Connect or Refresh tap, automatically reopened the replacement USB session, automatically repopulated fresh EQ + DEVICE state, showed no Applying/Flashing transaction, and showed no cached-state push to hardware.

**Automatic physical reattach result: PASS on exact signed `a3bc174`.**

### Editor native-step + Automatic preference regression fixes — PASS

Exact signed owner-tested source:

`51e1b74c7559fab6e0b994dce576891c3f082194`

This candidate fixed two later regressions:

- Black Pearl manual EQ editing normalizes ordinary displayed/manual Gain and Q input to the nearest representable native step before strict validation, preventing false rejection caused only by display rounding while retaining real range/representability checks;
- temporary My EQs / EQ Library output selection no longer overwrites the saved **Automatic / Manual** output-behavior preference.

All exact-head automated/security/catalog/signing gates passed on `51e1b74`, and the project owner subsequently reported **everything looks good** after focused physical/app testing. Evidence category: **OWNER-REPORTED PASS** for these two regressions.

### EQ Library Restore defaults — PASS

Exact signed source and physical-test pin:

`b0340842dd88dc85613d9441fcc72ceadf877b20`

Immutable signed APK:

`https://raw.githubusercontent.com/weekssa/OPRA-EQ-for-UAPP/mobile-test-apk/candidates/EQ-Library-v0.6.0-beta-b034084.apk`

APK SHA-256:

`dcae4f54881976af70e93c2c796b6993697d1a0a8f333698e9d925933c61ff37`

Signer:

`CN=OPRA EQ for UAPP, O=weekssa`

Signer certificate SHA-256:

`65c1c1256dae3c49e3548f334c91f0ba991969e9be9e0b223ba4e253d2114747`

Exact-source software gates passed before the physical test:

- Android CI #1505 / run `34896764825` — **PASS**;
- CodeQL #1387 / run `34896764843` — **PASS**;
- Catalog currentness CI #1775 / run `34896764824` — **PASS**;
- Priority community coverage CI #1260 / run `34896764835` — **PASS**;
- Signed EQ Library Beta Candidate #1180 / run `34896759539` — **PASS**;
- signed workflow gate `:app:testDebugUnitTest :app:lintDebug :app:assembleRelease` — **PASS**;
- signed artifact ID `10369545509`;
- artifact ZIP digest `sha256:ddc58020b52158f6e85f7fcc32ced41b9c284c453da128375aceee645f4b0c61`;
- signer certificate matched the repository-pinned release identity.

On 2026-09-14 the project owner completed the focused Pixel 9 / TRN Black Pearl Restore-defaults test on this exact signed candidate and reported **SUCCESS**. Evidence category: **OWNER-REPORTED**.

The passed owner test covers the approved app-owned restore contract:

- Cancel made no DEVICE or EQ change;
- checkbox OFF restored displayed **50% / FAST-LL / HIGH / CLASS AB / Centered / microphone 0 dB** while leaving the non-flat hardware EQ unchanged;
- the restored DEVICE values persisted through unplug/reconnect and fresh automatic read;
- checkbox ON restored the same DEVICE targets and reset the hardware EQ to **Flat**;
- saved My EQs remained unchanged;
- the restored DEVICE state and Flat EQ persisted through reconnect.

**Black Pearl EQ Library Restore defaults result: PASS on exact signed `b0340842`.**

This qualifies the app-owned Restore-defaults behavior. It does **not** establish the selected values as TRN factory defaults.

### Premium UX checkpoint — PASS

Exact signed source and owner-tested physical/usability pin:

`b952aed8d410fe215ac38292d035aec1b16ee2ef`

Immutable signed APK:

`https://raw.githubusercontent.com/weekssa/OPRA-EQ-for-UAPP/mobile-test-apk/candidates/EQ-Library-v0.6.0-beta-b952aed.apk`

APK SHA-256:

`28109cae0658ec292d6b8fc3bd684a6c9cfcb69e1f4cc0c5ef0adc445eb54285`

Signer:

`CN=OPRA EQ for UAPP, O=weekssa`

Signer certificate SHA-256:

`65c1c1256dae3c49e3548f334c91f0ba991969e9be9e0b223ba4e253d2114747`

Exact-source automated gates:

- Android CI #1518 / run `34906300036` — **PASS**;
- CodeQL #1400 / run `34906300038` — **PASS**;
- Catalog currentness CI #1793 / run `34906299963` — **PASS**;
- Priority community coverage CI #1278 / run `34906300033` — **PASS**;
- Signed EQ Library Beta Candidate #1193 / run `34906295337` — **PASS**.

On 2026-09-14 the project owner reviewed this exact signed checkpoint on Pixel 9 + TRN Black Pearl and reported **“Everything works great - pass.”** Evidence category: **OWNER-REPORTED**.

The focused pass covered the premium items implemented through this checkpoint, including the compact Target action context, the one-connection navigation model, flatter grouped Black Pearl DEVICE rows, Restore-defaults presentation, durable Automatic output behavior, and the Black Pearl ordinary-decimal EQ-editor regression path.

**Premium UX checkpoint result: PASS on exact signed `b952aed8`.**

Later commits through `a2c12c9b9f0f9a2a977884f92e7e5add24999bbe` modify only `docs/V0.6_PREMIUM_UX_AUDIT_BLUEPRINT.md`; they do not replace or invalidate the `b952aed8` physical evidence pin.

## Black Pearl qualification boundaries

- **Read-only DEVICE state — PASS**, pinned to `d3d227de45751f6606b75637a2fd311249afa88d`.
- **DAC reconstruction-filter write — PASS**, pinned to `f7a3179d8124c70175e377c8673958e2c83d28a3`.
- Existing Black Pearl Flash and **Reset EQ to flat** protocol behavior remains the qualified foundation.
- **EQ Library Restore defaults — PASS**, pinned to exact signed `b0340842dd88dc85613d9441fcc72ceadf877b20`.
- Premium presentation checkpoint through blueprint items 1–3 — **PASS, OWNER-REPORTED**, pinned to exact signed `b952aed8d410fe215ac38292d035aec1b16ee2ef`.
- Remaining normal DEVICE controls retain their software gates plus recorded owner-reported normal-use evidence; evidence categories are not silently upgraded.
- A true TRN whole-device factory-reset command/default table is still **not established**.
- The approved app-owned Black Pearl Restore-defaults preset is displayed 50% volume, FAST-LL, HIGH gain, CLASS AB, centered balance, **0 dB microphone gain**, plus an optional OFF-by-default **Also reset EQ to flat** checkbox.

The maintained research/transaction boundary remains in `docs/BLACK_PEARL_V0.6_FACTORY_DEFAULTS_RESEARCH.md`.

## FiiO JA11 status

The JA11 software surface uses the shared My DAC/session/status framework and its independently established native behavior, including native `0..60` volume semantics. Exact physical hardware qualification remains pending until the device is available. No Black Pearl-specific units, reset defaults, or commands are projected onto JA11.

JA11 remains explicitly labeled **Hardware validation pending**; do not claim physical qualification until its exact-device checklist passes.

## Current software state

The Black Pearl Restore-defaults behavior is implemented and physically qualified on the exact signed `b0340842` candidate. The approved premium presentation items 1–3 are physically/usability qualified on exact signed `b952aed8`.

The implementation deliberately:

- lowers Black Pearl playback to a conservative qualified minimum before level-sensitive restore writes;
- routes every selected target through the normal verified DEVICE path, with already-matching values allowed to resolve as verified no-ops;
- stops on stale session, transfer failure, readback mismatch, unrelated-state change, or other verification failure;
- never automatically retries a setting write;
- restores microphone gain to **0 dB**;
- restores the final whole-dB playback setting that displays as **50%**;
- keeps EQ flattening optional and separate;
- keeps target selection as action context rather than My EQs ownership state;
- keeps direct Flash on the EQ surfaces instead of recreating a My DAC chooser.

The device-agnostic My EQs / Needs attention work remains software-validated and documented. No hardware behavior was added to those library changes.

## Current blocker / next milestone

The v0.6.0 closeout is complete. Remaining work is optional follow-on polish and future hardware qualification:

- standardize any later row/card spacing, typography, icons, and action emphasis improvements across My EQs / EQ Library;
- continue polishing the Needs attention recovery surface and accessibility/large-font/TalkBack behavior as future releases;
- keep FiiO JA11 labeled hardware-validation-pending until its physical checklist passes.

No remaining v0.6.0 blocker requires owner action. A new Pixel 9 / TRN Black Pearl physical checkpoint is needed only if a future change reaches qualified behavior or materially changes the maintained UX.

## Release gate

Public v0.6.0 release, exact-head provenance, and required owner authorization are complete. The existing Black Pearl reconnect, Restore-defaults, and premium checkpoint gates remain passed.


## Final exact-head provenance closeout

The corrective physical PASS was completed on exact signed `eb1980076009001b5216ffbb531de8a28a4780eb`. The documentation closeout source is `bda79f83fc69701426dd09e418ed993583a521ca`; it changes maintained evidence/status records only.

Final exact-head gates on `bda79f83`:

- Android CI #1539 — **PASS**.
- CodeQL #1421 — **PASS**.
- Catalog currentness CI #1827 — **PASS**.
- Priority community coverage CI #1312 — **PASS**.
- Signed EQ Library Beta Candidate #1214 — **PASS**.
- APK: https://raw.githubusercontent.com/weekssa/OPRA-EQ-for-UAPP/mobile-test-apk/candidates/EQ-Library-v0.6.0-beta-bda79f8.apk
- APK SHA-256: `721b750a9d81217413b92556166f6b3a349aef5213ebdebe157c534254dd11f6`.
- Signed artifact ID: `10400741522`; ZIP digest: `sha256:4efce0cca8cd0c159eb82cd76148c9678659bb10053ad79f4bcecabf8f079b34`.
- Signer certificate remained `65c1c1256dae3c49e3548f334c91f0ba991969e9be9e0b223ba4e253d2114747`.

The final provenance gates are complete. PR #16 was merged, tag `v0.6.0` was created, and the public release is available at https://github.com/weekssa/OPRA-EQ-for-UAPP/releases/tag/v0.6.0. FiiO JA11 remains hardware-validation-pending.
