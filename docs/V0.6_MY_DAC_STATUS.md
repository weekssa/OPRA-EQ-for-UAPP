# EQ Library v0.6.0 — My DAC current status

Status date: 2026-09-13

This document is the concise current-state companion to `docs/V0.6_MY_DAC_IMPLEMENTATION_PLAN.md`, `docs/V0.6_MY_DAC_APPROVED_DESIGN.md`, and the maintained project runbook. It records what is implemented, what is physically proven, and what remains blocked.

## Current branch

- Repository: `weekssa/opra-eq-for-uapp`
- Branch: `v0.6-my-dac`
- Draft PR: #16
- Product version: `0.6.0`
- Public release remains `v0.5.0`; v0.6 is not published.

Behavior checkpoint `15694b0034d4191ab2cac10b592f122587b9bcc7` passed:

- Android CI #1238 — PASS
- CodeQL #1119 — PASS
- Catalog currentness CI #1350 — PASS
- Priority community coverage CI #838 — PASS

Later status/checklist documentation commits do not supersede that behavior checkpoint or the exact physical test candidate recorded below.

## Main/catalog reconciliation

Current `main` through `bc0de4101d204a9af614b96b1df5382ee5f7af19` was reconciled into `v0.6-my-dac` by merge commit `2863bba7262844bfe32857c09607c37eba8f4505`.

The 15 main-side commits after the v0.6 merge base had net changes only in:

- `.github/workflows/dependency-submission.yml`;
- `catalog/catalog.json`;
- `catalog/discovery/general_github_candidates.json`;
- `catalog/discovery/github_candidates.json`;
- `catalog/discovery/headphone_identity_audit.json`;
- `catalog/source_health.json`.

None of those six paths had v0.6 branch modifications after the merge base. The merge therefore takes the current main versions of those files while preserving all v0.6 implementation/docs. After the merge, `v0.6-my-dac` was 0 commits behind `main`.

Post-reconciliation validation on branch head `6d9e997354b3e5678773da6f727428b335b12a0b` completed successfully:

- Android CI #1261 — PASS (unit tests, lint, debug assembly, release assembly)
- CodeQL #1142 — PASS
- Catalog currentness CI #1374 — PASS
- Priority community coverage CI #861 — PASS

This confirms the reconciled branch remains software-green, but it does not replace the final exact release-candidate/signing gate.

## Implemented v0.6 My DAC behavior

### Session / navigation

- supported-DAC recognition is separate from USB connection state;
- My DAC appears immediately beside My EQs after a supported DAC is recognized;
- My DAC remains available for the current app session after disconnect and shows stale/Last-read state instead of making navigation jump;
- supported USB attach uses Android's normal permission/default-app flow;
- an already-open app uses the nonblocking **Open My DAC** prompt;
- connected-DAC context does not silently change the separate global active output.

### EQ state

- read-only native hardware EQ snapshots;
- deterministic Black Pearl native fingerprints;
- Flat / Exact / ambiguous Exact / Unknown matching;
- Modified lineage only after a verified in-app edit that began from a known saved representation;
- reconnect or any outside native change invalidates Modified lineage back to Unknown;
- ordinary Black Pearl playback/global gain is not used to invent EQ/source identity.

### Visual EQ / editor

- deterministic combined-response graph;
- compact read-only hardware graph and larger editor graph;
- tap-only band selection; graph dragging never edits or writes hardware;
- exact frequency/gain/Q entry and supported filter-type selection;
- All Bands view;
- deterministic headroom assessment from the complete planned response;
- separate blocking device limits, caution range, and headroom status;
- Review before Apply;
- fresh-baseline checks, safe gain ordering, device write, readback, and verification;
- requested local state is never reported as current hardware until verification succeeds.

### My DAC EQ actions

- **Change EQ** uses the Black Pearl-specific My EQs collection rather than the unrelated global active-output collection;
- candidates show deterministic Exact / Optimized / Not suitable status;
- selection opens an explicit Flash review; it never writes immediately;
- **Edit EQ** uses the verified editor/apply transaction;
- **Save DAC EQ** captures current native state as a Personal EQ with capture provenance and optional real headphone association or intentional unassociated state;
- exact existing matches link instead of creating duplicates;
- **Reset EQ to flat** is a secondary confirmed action that reuses the already-qualified Black Pearl reset transaction and does not arbitrarily reset ordinary playback volume.

### DEVICE architecture

- `DacControlRepository` remains separate from `HardwareEqRepository`;
- Compose receives typed capability/presentation state and contains no protocol constants;
- unqualified DEVICE controls are structurally absent rather than exposed as speculative controls;
- Black Pearl has a dedicated **Hardware qualification · read-only** panel for firmware/version, DAC reconstruction filter, gain mode, amplifier topology, microphone gain, left/right balance, and playback/global gain;
- all candidate fields must be read successfully from one current session or the qualification read fails;
- no new DEVICE write control is production-exposed.

## Exact Black Pearl read-only hardware candidate

The completed read-only physical qualification is pinned to:

- source commit: `d3d227de45751f6606b75637a2fd311249afa88d`
- app version: `0.6.0`
- immutable APK: `https://raw.githubusercontent.com/weekssa/OPRA-EQ-for-UAPP/mobile-test-apk/candidates/EQ-Library-v0.6.0-beta-d3d227d.apk`
- APK SHA-256: `ea8235ee1ba2828873b488054b25d40c917b844c2d76a813bf9e038cf31c8ee5`
- signer certificate SHA-256: `65c1c1256dae3c49e3548f334c91f0ba991969e9be9e0b223ba4e253d2114747`
- Android CI #1226 — PASS
- CodeQL #1107 — PASS
- Catalog currentness #1331 — PASS
- Priority community coverage #819 — PASS
- Signed EQ Library Beta Candidate #901 — PASS

The moving `EQ-Library-mobile-test.apk` / `EQ-Library-v0.6.0-mobile-test.apk` filenames are not qualification provenance. Do not silently substitute a later development APK into that qualification record. A behavior-changing replacement candidate must be explicitly pinned and the affected physical evidence repeated.

## Black Pearl read-only qualification result

**PASS — 2026-09-13.** The pinned candidate has now passed the complete maintained read-only DEVICE qualification on the project owner's Pixel 9 / TRN Black Pearl.

Verified behavior includes:

- in-app recognition of **TRN Black Pearl** and textual **Connected** state;
- one complete same-session read of all seven candidate fields;
- independent semantic agreement for firmware `0.6`, `Fast-PC`, `HIGH`, `CLASS AB`, mic `0 dB`, centered balance, and playback/global raw `512` / `+2.00 dB`;
- independent-controller playback presentation `Volume 63%`, reconciled to raw `512` through the controller's maintained linear presentation mapping;
- three consecutive stable/non-destructive qualification reads;
- external filter change detected by a fresh EQ Library read, followed by verified restoration to `Fast-PC`;
- disconnect keeps My DAC session-visible while marking retained values stale and preventing a current read;
- reconnect does not promote old data current before a new read;
- fresh post-reconnect read restores **Current session read** and the expected baseline;
- final independent no-write comparison reconfirming `v0.6`, `FAST-PC`, `High`, `Class-AB`, mic `0 dB`, `Center`, and `Volume 63%` with no unexpected persistence/state change.

The optional mid-read hardware interruption exercise was intentionally not performed; automated session-generation and field-read failure coverage is green.

This qualification **only qualifies read semantics**. It does not authorize or qualify any new DEVICE write command.

## Still intentionally absent / unqualified

The following remain unqualified and must not be exposed as normal write-capable DEVICE controls merely because reads work:

- DAC reconstruction-filter writes;
- gain-mode writes;
- amplifier-topology writes;
- balance writes;
- microphone-gain writes;
- playback/global-gain DEVICE writes;
- cached-state restore on reconnect;
- firmware/bootloader/cross-flash writes;
- raw register console or USB identity mutation.

FiiO JA11 and stock JCALLY JM12 remain hardware-validation pending.

## Next-build implementation gate

The project owner has explicitly asked to review and provide feedback before the next product/code implementation cycle begins. No next-cycle product or code implementation should start until the proposed UX/behavior is shown in plain language and the owner approves it. Documentation/release-closeout work may continue without implying approval for new behavior.

## Release blocker

See `docs/V0.6_RELEASE_CHECKLIST.md` for the controlled closeout sequence.

v0.6.0 must not merge/publish until the final exact release candidate receives the required Android/security/catalog/signing validation, the final Pixel 9 / Black Pearl release smoke passes on that exact signed APK, maintained docs/changelog/release notes are synchronized, and the project owner explicitly authorizes publication.
