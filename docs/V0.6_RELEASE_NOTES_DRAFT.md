# EQ Library v0.6.0 — release notes draft

Status: **DRAFT — not approved for publication yet**.

Use this as source material for the eventual GitHub Release / What's New text after final exact-candidate validation. Do not claim hardware qualification beyond the maintained evidence below.

## What's new

### My DAC

EQ Library can now recognize supported DACs and add a dedicated **My DAC** area for actual connected-hardware state.

When a supported DAC is recognized, navigation becomes:

**My EQs · My DAC · EQ Library · Settings**

My DAC stays available for the current app session after disconnect so the app can clearly distinguish current hardware from **Last read** state instead of making navigation jump.

### One shared DAC connection

A supported DAC now uses one app-wide session shared by **My EQs**, **My DAC**, and **EQ Library**.

After connection or reconnection, EQ Library automatically reads the current EQ and supported DEVICE state. You do not need to reconnect or perform a second read merely because you opened My DAC.

Successful Flash, reset, editor Apply, and DEVICE changes automatically refresh the affected hardware state. Manual **Refresh** remains available for changes made outside EQ Library.

### One device-agnostic My EQs library

**My EQs is now one local library regardless of the current EQ target.**

Changing the target can change compatibility, fidelity, the derived representation, Export availability, or Flash availability, but it does not change which headphones and EQs you saved.

Managed headphone selections, Favorites, Personal EQs, captured DAC EQs, and General EQs remain the same local items when you move between UAPP, Black Pearl, FiiO, or another target.

Saving or adding an EQ to My EQs is deliberately separate from **Export** and **Flash**. Save changes local library state only; it does not silently write a file or hardware.

### Needs attention recovery

v0.6 adds a persistent **Needs attention** recovery surface for EQ Library-created preset files that the app still owns but can no longer confidently associate with a current My EQ item.

Recovery is conservative:

- only exact app-owned/persisted-access files are considered;
- supported parametric EQ values are strictly parsed and preserved;
- you provide missing Manufacturer, Model, and EQ name before recovery;
- recovered files become normal device-agnostic Personal EQs;
- malformed or unsupported data stays visible instead of being silently discarded or partially converted;
- deletion is limited to the exact app-owned file and requires an explicit user action.

EQ Library does not crawl or delete arbitrary files outside its managed ownership boundary.

### Automatic EQ targeting

The default behavior is **Automatic (recommended)**. When exactly one supported DAC is present, EQ Library can use that DAC as the effective EQ target without requiring you to preselect it first. With no supported DAC present, the app falls back to your saved **Default EQ target**. If more than one supported DAC is present, EQ Library does not guess.

A persistent **Manual** override remains available in Settings. The setting controls how EQs are prepared, exported, and flashed; it does not change Android's audio output route or which EQs belong to My EQs.

### Read the actual Black Pearl EQ

For TRN Black Pearl, My DAC reads the native hardware EQ and can show:

- current versus Last-read state;
- Flat, exact saved match, modified-known, ambiguous exact, or Unknown identity state;
- active slot and filter count;
- current playback/global-gain context;
- a combined parametric response graph;
- exact native differences when a known saved EQ has been modified through EQ Library.

EQ identity is conservative: cold-start similarity is never treated as source attribution, and ordinary playback volume is not used to invent an EQ match.

### Flash where the EQ already lives

My DAC no longer contains a second **Change EQ** chooser.

- Flash saved EQs from **My EQs**.
- Flash a compatible library EQ directly from **EQ Library** when exactly one supported DAC session is connected.
- Use **My DAC -> EQ** to inspect what is actually on the hardware, edit/capture it, or reset it.

Flash still uses the same deterministic target representation, safety checks, and readback verification underneath.

### Edit EQ on the Black Pearl

The Black Pearl editor starts from a fresh verified hardware read and provides:

- tap-to-select response graph markers;
- exact filter type, frequency, gain, and Q editing;
- All Bands view;
- live deterministic combined-response preview;
- headroom assessment from the complete planned response;
- separate device-limit, caution, and headroom warnings;
- Review before Apply;
- hardware readback verification after Apply.

Dragging the graph never changes hardware. Local edits remain local until explicit Review -> Apply.

### Headroom-aware Apply

EQ Library evaluates the complete planned response rather than simply looking for the largest positive band gain. When additional safe attenuation is needed, the editor makes that requirement visible before a write.

For Black Pearl, EQ Library continues to distinguish its tracked EQ safety adjustment from the DAC's ordinary playback level and preserves the established safe write ordering.

### Save DAC EQ

Unknown or known-modified Black Pearl hardware state can be captured as a local **Personal EQ**.

Captured EQs preserve native filter values and device provenance. The DAC identity is provenance only; after capture the EQ behaves like any other Personal EQ and remains available regardless of the selected target. You can associate a capture with a real saved/library headphone or intentionally leave it unassociated. EQ Library does not invent a creator/source association, and an exact existing saved match is linked rather than duplicated.

### Reset EQ to flat and restore Black Pearl defaults

My DAC includes the qualified Black Pearl **Reset EQ to flat** workflow as a secondary confirmed action. It replaces the hardware EQ with the verified flat state while preserving the established tracked-gain safety behavior rather than arbitrarily resetting listening volume.

v0.6 also includes a separate, truthfully labeled **Restore defaults** action for Black Pearl. This is an **EQ Library-owned preset**, not a claim about TRN factory defaults. It restores:

- Volume: **50%**;
- DAC filter: **FAST-LL**;
- Gain: **HIGH**;
- Amplifier: **CLASS AB**;
- Balance: **Centered**;
- Microphone gain: **0 dB**.

An optional **Also reset EQ to flat** checkbox starts OFF. With it OFF, the current hardware EQ is left unchanged. With it ON, EQ Library reuses the separately qualified Reset EQ to flat transaction. Saved EQs in My EQs are never deleted or modified by either path.

The Restore-defaults flow reuses the established Black Pearl write/persist/readback transaction, lowers playback to a conservative safe intermediate level first, establishes the final 50% presentation last, does not automatically retry failed writes, and reports success only after requested state is verified.

A true TRN factory-reset command/default table is still not established, so EQ Library does not present this app-selected preset as a TRN factory reset.

### A simpler DEVICE screen

Black Pearl DEVICE settings are presented as one concise current-value list instead of a duplicated overview plus editor.

Editable rows are the controls themselves. Routine choices apply through the safe write/readback transaction immediately; settings that may materially change listening level keep a short confirmation. The app does not expose protocol mechanics as normal UI.

Black Pearl ordinary playback is presented simply as **Volume · N%**. The hardware's native raw/dB register remains an internal transaction detail rather than a user-visible baseline. After a verified Apply, fresh readback becomes the new current volume; there is no persistent user-facing delta from the value that happened to be present when the screen opened.

DEVICE changes use a common progress pattern across supported DACs: **Applying <setting>…**, then a brief verified **<Setting> updated ✓**, or a truthful error/current-state message. The app no longer labels the retained pre-write snapshot `Last read` while a normal write is actively being verified.

The current Black Pearl surface includes established state for volume, DAC reconstruction filter, gain mode, amplifier topology, balance, microphone gain, firmware, and USB audio mode according to the capability/qualification policy for the exact device.

### Black Pearl USB audio mode

EQ Library detects the active **UAC 1.0** or **UAC 2.0** mode from the current USB AudioControl descriptors. There is no speculative in-app UAC switch.

The DEVICE screen includes the concise manual startup sequence:

- UAC 1.0: disconnect the Black Pearl, leave headphones connected, hold **+** and **−**, reconnect USB, then release after power/enumeration;
- UAC 2.0: reconnect normally without holding the buttons;
- EQ Library automatically reads the replacement session and displays the detected mode.

### FiiO JA11 foundation

v0.6 also builds the shared My DAC/session framework for the FiiO/JadeAudio JA11. Software-established device state and controls use the same capability-driven architecture, and EQ selection/Flash belongs to My EQs / EQ Library rather than a duplicate My DAC chooser.

JA11 uses the same shared DEVICE progress/current/stale presentation while retaining its own independently established native `0..60` volume scale. Device-specific units and reset semantics are never borrowed from Black Pearl.

JA11 remains **Hardware validation pending** until the exact physical checklist can be run on the device.

## Safety and privacy

- No account, analytics, telemetry, or cloud backend is added.
- My EQs, recovery state, and My DAC state stay local.
- Reconnect never pushes cached values back to hardware.
- Requested editor or DEVICE state is not reported as current until verified hardware readback succeeds.
- Normal DEVICE writes are not silently retried after transfer.
- Firmware/bootloader/cross-flash writes, raw register consoles, and USB-identity mutation are not exposed.
- Unsupported controls disappear rather than being guessed from a similar device.
- Factory/default values are not inferred from another DAC, another firmware, an observed test baseline, or a third-party button label.
- Needs attention does not scan arbitrary external storage and can delete only an exact app-owned managed document.

## Hardware validation status

Current maintained evidence before publication:

- TRN Black Pearl existing Flash/Reset protocol: hardware qualified and preserved as the regression anchor.
- TRN Black Pearl v0.6 read-only DEVICE state: **PASS**.
- TRN Black Pearl DAC reconstruction-filter write: **PASS**.
- TRN Black Pearl finished connected-DAC UX checks: **PASS** with maintained owner-reported/screenshot evidence as recorded in the release checklist.
- TRN Black Pearl automatic physical reconnect: **PASS, OWNER-REPORTED**, on exact signed candidate `a3bc1740ed44892f4e6b78f9d0a359e2a87ef663`.
- TRN Black Pearl EQ Library **Restore defaults**: **PASS, OWNER-REPORTED**, on exact signed candidate `b0340842dd88dc85613d9441fcc72ceadf877b20`.
- Restore-defaults candidate APK SHA-256: `dcae4f54881976af70e93c2c796b6993697d1a0a8f333698e9d925933c61ff37`.
- Restore-defaults tested targets: 50% / FAST-LL / HIGH / CLASS AB / Centered / microphone 0 dB; checkbox OFF preserved the existing EQ, checkbox ON reset EQ to Flat, and restored state persisted through reconnect.
- TRN Black Pearl whole-device **TRN factory-reset** semantics: **not established**. The app-owned Restore-defaults preset must not be described as TRN factory state.
- FiiO/JadeAudio JA11: **Hardware validation pending**.

Documentation-only closeout after the physical PASS does not replace the pinned `b0340842` hardware evidence unless connection ownership, read timing, write sequencing, persistence, reset values/order, EQ-reset behavior, verification, or session semantics change.

## Attribution

EQ Library continues to preserve source/creator attribution and does not imply endorsement by OPRA, Roon Labs, USB Audio Player PRO, ToneBoosters, TRN, FiiO, or other referenced products/projects.

## 2026-09-15 corrective checkpoint

The owner-reported `431cbfa` test failed Restore defaults (premature stop at 0%) and page-density review. The current corrective work and genuine-failure policy are maintained at `docs/V0.6_MY_DAC_STATUS.md`. Restore completion is tied to the exact write cycle and original USB session; all final targets must match. No failed setting is automatically retried. Managed detail and General EQ headers/actions are compact and scrollable. A new exact signed beta needs focused physical review; historical PASS pins do not qualify these corrections. PR #16 remains open/draft and v0.5.0 remains public.


## 2026-09-15 corrective candidate — PASS

The project owner completed the focused Pixel 9 / TRN Black Pearl retest on exact signed source `eb1980076009001b5216ffbb531de8a28a4780eb` and reported **SUCCESS**.

The retest confirmed:

- Restore defaults completed normally on the first attempt and reached **50% / FAST-LL / HIGH / CLASS AB / Centered / microphone 0 dB**.
- The optional EQ-flat behavior and saved My EQs remained protected as specified.
- Reconnect persistence remained correct.
- The revised My EQs managed-headphone detail and General EQs pages were materially less cramped, with the revised action/help hierarchy.

Evidence category: **OWNER-REPORTED**.

Exact signed APK: https://raw.githubusercontent.com/weekssa/OPRA-EQ-for-UAPP/mobile-test-apk/candidates/EQ-Library-v0.6.0-beta-eb19800.apk

APK SHA-256: `dabf4bcdddf69853b09793f5a94bec0a3af7efb430f1cdfe26ffc35a93b783ad`

The exact candidate passed Android CI #1538, CodeQL #1420, Catalog currentness CI #1826, Priority community coverage CI #1311, and Signed EQ Library Beta Candidate #1213. The signing workflow verified the pinned certificate `65c1c1256dae3c49e3548f334c91f0ba991969e9be9e0b223ba4e253d2114747`.

This PASS closes the corrective Black Pearl physical gate. It does not establish TRN factory-default semantics and does not qualify FiiO JA11 hardware behavior. PR #16 remains open and draft; merge, release, and publication remain explicit owner-authorization gates.

