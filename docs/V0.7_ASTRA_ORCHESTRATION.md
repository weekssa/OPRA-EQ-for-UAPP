# v0.7 Astra Orchestration Log

Status: active orchestration record for PR #23 (`feature/v0.7-ew300-luna-high`).

This file records bounded specialist tasks, their acceptance criteria, synthesis, and immediate close-out. The current chat environment does not expose a callable Astra endpoint, so each block is maintained as an Astra-ready handoff and executed through the available project/GitHub research path. A task is closed as soon as its acceptance criteria are satisfied; no specialist task is intentionally left lingering.

Repository boundary: `weekssa/opra-eq-for-uapp` is the only writable repository. `weekssa/opra-uapp-converter` and all third-party repositories are read-only references.

Rollback anchor before orchestration: `backup/v0.7-ew300-pre-orchestrator-20260922` at `54e238f2abe79a9b041a68e6c8d8d7b714df6a1d`.

Privacy boundary: the exact qualified unit serial is controlled qualification evidence and is intentionally omitted from this orchestration/public-support document. Runtime authorization remains governed by the existing exact-device qualification code and maintained evidence; do not broaden support from VID/PID alone.

## Astra Task 1 — PaperFinder-equivalent EW300 capability crosswalk

**Status:** CLOSED — PASS

### Objective

Complete an exact-device capability crosswalk for the qualified SIMGOT EW300 DSP against the Black Pearl/My DAC capability inventory, including an explicit independent-volume investigation. Determine which capabilities are safe product features for v0.7 without inheriting behavior from another DAC or a generic KT02H20-family implementation.

### Context & Inputs

- Governing sources: `AGENTS.md`, `docs/CHATGPT_PROJECT_RUNBOOK.md`, `docs/ARCHITECTURE.md`, `docs/V0.6_MY_DAC_APPROVED_DESIGN.md`, `docs/V0.7_PRODUCT_SUCCESS_CRITERIA.md`, `docs/EW300_CAPABILITY_MATRIX.md`, `docs/EW300_VALIDATION_LEDGER.md`, and accepted physical evidence.
- The exact qualified device fingerprint remains authoritative; public orchestration text intentionally omits private unit identity.
- Accepted evidence already establishes the five-band Peak path, reported playback/global-gain state, persistence, guarded Apply/Flash/Reset, reconnect/final readback, and validation-only exact-baseline restoration.
- Register `0x66` is verified playback/global-gain state but was not previously qualified as an independent user-facing volume control.
- Public/reference research may inspect exact-product material and licensed/reference implementations. Generic chipset behavior is discovery evidence only.

### Out-of-Scope

- No hardware mutation, Save qualification, Apply, Flash, Restore, Reset, or random register probing.
- No firmware/bootloader/recovery/cross-flash/erase/calibration work.
- No copied commands, ranges, defaults, persistence semantics, or controls from Black Pearl, JA11, or generic KT02H20 implementations without exact EW300 evidence.
- No claim that absence of evidence means the hardware does not provide a feature.

### Acceptance Criteria

- [x] Enumerate native EQ/filter families; band count/unused-band behavior; read/edit/capture/Apply/Flash/Reset/persistence/restoration; standalone volume vs global gain; DAC filter; gain mode; amplifier/output topology; balance; microphone/headset controls; UAC behavior; identity; firmware/version/update; connection/permission/reconnect/freshness/manual refresh; feedback/safety.
- [x] Classify every category as `SUPPORTED_AND_IMPLEMENTED`, `HARDWARE_DOES_NOT_PROVIDE`, `INSUFFICIENT_EVIDENCE`, or `UNSAFE_OR_OUT_OF_SCOPE`.
- [x] Use `HARDWARE_DOES_NOT_PROVIDE` only with affirmative exact-device evidence.
- [x] Resolve whether `0x66` can be promoted to an independent user-facing volume control.
- [x] Review third-party license/provenance before relying on implementation facts.
- [x] Update the capability matrix only where research strengthens the evidence.
- [x] Close immediately once the crosswalk is defensible; unresolved capabilities remain `INSUFFICIENT_EVIDENCE`.

### Close-out synthesis

- Five-band native Peak EQ and the previously qualified guarded mutation/persistence path remain the supported EW300 control surface.
- Generic KT02H20 documentation corroborates `0x66` as digital DAC/playback gain, but exact EW300 evidence does not establish an independent software-volume UX, safe range, persistence contract, or standalone write semantics. No volume control is exposed.
- Exact-product material establishes a microphone and media/call control but does not establish app-controlled microphone gain or headset configuration.
- DAC filter, gain mode/PGA, amplifier/output topology, balance, microphone gain, UAC switching, firmware/update, routing, and factory-default controls remain `INSUFFICIENT_EVIDENCE` unless separately marked unsafe/out of scope.
- Generic-family references reinforce that shared VID/PID values are not sufficient model identity. No third-party implementation code was copied.

**Agent/task closed immediately after acceptance.**

## Astra Task 2 — shared My DAC EW300 UX parity implementation/review

**Status:** CLOSED — PASS

### Objective

Close the concrete EW300 My DAC parity gaps in the approved v0.7 acceptance contract while preserving the qualified protocol/mutation path: shared compact response graph, truthful EQ-state classification, manual Refresh through the authoritative session, concise DEVICE copy, accessibility, and regression coverage.

### Context & Inputs

- Governing UX: `docs/V0.6_MY_DAC_APPROVED_DESIGN.md` and `docs/V0.7_PRODUCT_SUCCESS_CRITERIA.md`.
- Shared graph: `ui/components/DacEqResponseGraph.kt`; deterministic response authority stays in the domain response evaluator.
- Shared authoritative session/read path: `HardwareEqRepository` + `DacSessionRepository`; Refresh must not create a second USB session.
- Exact matching uses `HardwareEqMatcher`; curve similarity is never source provenance.
- Task 1 closed with no additional independent EW300 DEVICE write control qualified.

### Out-of-Scope

- No change to EW300 wire protocol, write order, Save/reconnect invariants, mutation authorization, or physical qualification claims.
- No new volume, filter, gain-mode, topology, balance, microphone, UAC, firmware, routing, or factory-default controls.
- No duplicate EQ chooser or fuzzy source attribution.
- No physical mutation/retest.

### Acceptance Criteria

- [x] EQ overview uses the shared compact log-frequency response graph, 0 dB reference, active-band markers, and complete accessibility description.
- [x] State presentation truthfully supports Flat, deterministic exact saved match, ambiguous exact match, explicit-lineage-only Modified known, and Unknown.
- [x] Current versus Last read/stale state remains textually explicit.
- [x] Manual Refresh rereads via the existing authoritative session and never creates a second connection path.
- [x] Refresh exposes reading/failure/current feedback and cannot initiate mutation.
- [x] DEVICE is compact: verified playback/global gain, active Peak-band count, connection freshness, and gated validation evidence only.
- [x] No unqualified controls appear.
- [x] Unit/Compose tests cover graph/state/Refresh/stale-read behavior.
- [x] Capture copy agrees with canonical behavior: playback/global gain is device state and is not saved as Personal EQ identity.

### Close-out synthesis

- Added deterministic saved-EQ matching from the complete native five-band representation; playback/global gain remains outside EQ identity.
- Added Flat/exact/ambiguous/unknown presentation without fuzzy curve attribution.
- Added the shared response graph, markers, zero reference, and non-color accessibility description.
- Added EQ and DEVICE Refresh through the authoritative existing session.
- Condensed DEVICE presentation without introducing unqualified controls.
- Corrected Personal EQ capture copy in commit `f546055001987ecc22f4d4a30ff88baa8cf4c883` so it no longer implies playback/global gain is captured.
- Earlier focused Android CI on the UX implementation passed; final exact-head release gates remain part of the later release task.

**Agent/task closed immediately after acceptance.**

## Astra Task 3 — DataVoyager-equivalent shelf corpus reproduction and quality audit

**Status:** OPEN

### Objective

Independently validate low-shelf/high-shelf/mixed-shelf adaptation across representative EQ source paths, reproduce any real remaining defect, and add deterministic regression evidence. Change the optimizer only if the corpus demonstrates a concrete correctness or quality failure; otherwise strengthen tests/documentation without unnecessary algorithm churn.

### Context & Inputs

- Governing product contract: `docs/V0.7_PRODUCT_SUCCESS_CRITERIA.md` and the source-shelf/native-shelf distinction in `docs/EW300_CAPABILITY_MATRIX.md`.
- Shared optimizer/evaluator authority: `domain/kt02h20/Kt02h20FiveBand.kt`, `HardwareEqResponseEvaluator`, and `HardwareEqDeviceSpecs.SIMGOT_EW300`.
- Representative source families: OPRA, AutoEQ, community/general presets, strict text import, Personal EQ, and captured DAC EQ where deterministic repository paths exist.
- OPRA schema treats low/high shelves as Q-based filters; pass-filter slope semantics must not be invented for shelf profiles.
- Canonical production catalog data must not be edited merely to create tests.

### Out-of-Scope

- No claim of native EW300 low/high-shelf support; the product hardware path remains five Peak bands.
- No hardware mutation, Save, Apply, Flash, Restore, Reset, or physical test during corpus analysis.
- No canonical source mutation, silent unsupported-filter dropping, or weakened quality/safety gates.
- No broad optimizer rewrite unless a reproduced failure shows the current algorithm is wrong or materially unsafe.

### Acceptance Criteria

- [x] Corpus includes low shelf, high shelf, mixed shelves, shelves plus Peaks, an over-budget source, and an intentionally Not-Suitable unsupported-filter case.
- [x] Exercise representative OPRA, AutoEQ, community/general, strict import, Personal EQ, and captured-DAC paths.
- [x] Verify canonical source immutability and deterministic repeated output.
- [x] Verify Peak-only derived EW300 representation with no more than five bands.
- [x] Verify existing RMS and maximum response-error gates rather than accepting encode-only success.
- [x] Preserve truthful `Exact`/`Optimized`/`NotSuitable` classification.
- [ ] Complete exact-head CI after the corpus test-compilation compatibility fix.
- [ ] Close immediately after CI establishes whether a production-code change is actually necessary.

### Current finding

The current fitter evaluates the full canonical source response, derives a target-specific Peak-only representation, and enforces the existing RMS/max-error limits. The broader corpus has not yet demonstrated a production optimizer defect. A test-only compile incompatibility in the first corpus version was corrected without removing or weakening any assertion; exact-head CI is pending.

## Pending specialist tasks

- Astra Task 4 — regression/release evidence and metadata reconciliation.
- Astra Task 5 — signed beta provenance, final testing plan, and comprehensive handoff.
