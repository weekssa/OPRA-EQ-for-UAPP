# v0.7 Astra Orchestration Log

Status: active orchestration record for PR #23 (`feature/v0.7-ew300-luna-high`).

This file records bounded specialist tasks, their acceptance criteria, synthesis, and close-out. The chat environment does not expose a callable Astra endpoint, so each block is written as a complete Astra-ready handoff and executed through the available project/GitHub research path. Do not treat that limitation as permission to leave a task open: each block is closed immediately after its acceptance criteria are satisfied.

Repository boundary: `weekssa/opra-eq-for-uapp` is the only writable repository. `weekssa/opra-uapp-converter` and all third-party repositories are read-only references.

Rollback anchor before this orchestration: `backup/v0.7-ew300-pre-orchestrator-20260922` at `54e238f2abe79a9b041a68e6c8d8d7b714df6a1d`.

## Astra Task 1 — PaperFinder-equivalent EW300 capability crosswalk

**Status:** CLOSED — PASS

### Objective

Complete an exact-device capability crosswalk for the qualified SIMGOT EW300 DSP fingerprint against every relevant Black Pearl/My DAC capability category, including an explicit independent-volume investigation. Determine which capabilities are safe product features for v0.7 without inheriting behavior from another DAC or from generic KT02H20-family implementations.

### Context & Inputs

- Governing sources: `AGENTS.md`, `docs/CHATGPT_PROJECT_RUNBOOK.md`, `docs/ARCHITECTURE.md`, `docs/V0.6_MY_DAC_APPROVED_DESIGN.md`, `docs/V0.6_MY_DAC_IMPLEMENTATION_PLAN.md`, `docs/V0.7_EW300_DSP_IMPLEMENTATION_PLAN.md`, `docs/V0.7_PRODUCT_SUCCESS_CRITERIA.md`, `docs/V0.7_EW300_DSP_STATUS.md`, `docs/EW300_CAPABILITY_MATRIX.md`, and `docs/EW300_VALIDATION_LEDGER.md`.
- Exact authorized fingerprint: `vid=31b2|pid=111|manufacturer=LE XIAN|product=SIMGOT EW300 DSP|serial=2024-07-03-0000-0000-0000|interface=3`.
- Accepted evidence already establishes the five-band Peak path, reported playback/global-gain state, persistence, guarded Apply/Flash/Reset, and exact-baseline restoration. Do not repeat physical mutation or E001 Save qualification.
- Existing matrix says register `0x66` is verified playback/global-gain state but is not yet an independently qualified standalone playback-volume control.
- Public/reference research may inspect exact-device material and licensed/reference implementations such as `gxcreator/ktmicro-tools`, `jeromeof/devicePEQ`, and other relevant exact-device sources. Generic chipset behavior is discovery evidence only.
- The user wants all real device capabilities supported, but only when exact-device semantics and safety are evidenced.

### Out-of-Scope

- No hardware mutation, Save qualification, Apply, Flash, Restore, Reset, or random register probing.
- No firmware/bootloader/recovery/cross-flash/erase/calibration work.
- No copying commands, ranges, defaults, persistence semantics, or UI controls from Black Pearl, JA11, or a generic KT02H20 implementation without exact EW300 evidence.
- No app code changes in this research task.
- No claim that absence of evidence means the hardware does not provide a feature.

### Acceptance Criteria

- [x] Enumerate at minimum: native EQ/filter families; band count and unused-band behavior; read/edit/capture/Apply/Flash/Reset/persistence/restoration; standalone playback volume vs global gain; DAC filter; gain mode; amplifier/output-stage topology; balance; microphone/headset controls; UAC detection/switching/help; model/USB identity; firmware/version/update; connection/permission/reconnect/freshness/manual refresh; feedback/safety.
- [x] Classify every category as exactly one of `SUPPORTED_AND_IMPLEMENTED`, `HARDWARE_DOES_NOT_PROVIDE`, `INSUFFICIENT_EVIDENCE`, or `UNSAFE_OR_OUT_OF_SCOPE`.
- [x] Use `HARDWARE_DOES_NOT_PROVIDE` only with affirmative exact-device evidence.
- [x] Resolve whether `0x66` can be promoted to an independent user-facing volume control; family-level documentation alone is insufficient.
- [x] Review third-party license/provenance before relying on implementation facts.
- [x] Record any newly supported capability with exact semantics, range/units, read/write direction, persistence/readback, and safety evidence sufficient for implementation.
- [x] Update `docs/EW300_CAPABILITY_MATRIX.md` only where research materially changes or strengthens current evidence.
- [x] Close this task immediately after the crosswalk is defensible; any unresolved capability remains `INSUFFICIENT_EVIDENCE` and does not block Task 1 close-out.

### Close-out synthesis

- Exact-product material establishes a microphone and one media/call button but no in-line volume buttons. It does not establish a software DAC-volume contract.
- Generic KT02H20 documentation corroborates `0x66` as digital DAC/playback gain, but exact EW300 evidence still qualifies that field only as state used by guarded EQ transactions/restoration. No standalone volume row/write is promoted.
- Generic-family tools expose additional filters/gain/PGA/other controls; those remain discovery evidence only. Multiple products reuse `0x31B2` and even `0x31B2:0x0111`, reinforcing the exact fingerprint gate.
- `gxcreator/ktmicro-tools` is BSD-3-Clause; `Bukutsu/glacier-eq` is GPL-3.0 and was used only as research. No third-party code was copied.
- No additional EW300 DEVICE control met the exact-device product/safety threshold. All unresolved categories remain `INSUFFICIENT_EVIDENCE`, not “unsupported hardware.”

**Agent/task closed immediately after acceptance.**

## Astra Task 2 — shared My DAC EW300 UX parity implementation/review

**Status:** CLOSED — PASS

### Objective

Close the concrete EW300 My DAC parity gaps identified by the approved v0.7 acceptance contract while preserving the existing qualified protocol and mutation path: add the shared compact response graph, truthful EQ-state classification, compact manual Refresh through the authoritative session, concise DEVICE copy, and regression coverage.

### Context & Inputs

- Governing UX: `docs/V0.6_MY_DAC_APPROVED_DESIGN.md` and `docs/V0.7_PRODUCT_SUCCESS_CRITERIA.md`.
- Current EW300 product surface: `app/src/main/java/com/weekssa/opraeqforuapp/ui/screens/Ew300MyDacContent.kt`.
- Current shared graph: `ui/components/DacEqResponseGraph.kt`; deterministic response authority remains in domain response evaluation.
- Current EW300 snapshot/fingerprint: `HardwareEqSnapshotFactory.ew300`; `0x66` playback/global gain is deliberately excluded from EQ identity.
- Current authoritative session/read path: `HardwareEqRepository` + `DacSessionRepository`. Do not add a second USB session or reconnect ceremony for Refresh.
- Current exact-match machinery is `HardwareEqMatcher`; Modified-known state requires explicit lineage and must not be inferred from curve similarity.
- Task 1 closed with no additional product DEVICE control qualified.

### Out-of-Scope

- No change to the EW300 wire protocol, write order, Save/reconnect invariants, filter-family exposure, mutation authorization, or physical qualification claims.
- No new volume, DAC-filter, gain-mode, topology, balance, microphone, UAC, firmware, routing, or factory-default controls.
- No new navigation or duplicate EQ chooser.
- No fuzzy/curve-similarity source attribution.
- No physical hardware mutation or retest.

### Acceptance Criteria

- [x] EW300 EQ overview renders the same shared compact log-frequency response graph with 0 dB reference, active-band markers, and a complete accessibility description.
- [x] The overview truthfully classifies at least `Flat`, deterministic exact saved match when available, and `Unknown`; `Modified known` appears only when explicit lineage is actually available.
- [x] Current vs Last read/stale state remains textually explicit and the graph never turns stale/cached state into a “current” claim.
- [x] A compact manual **Refresh** rereads EW300 EQ/DEVICE state through the existing authoritative shared session; it does not create another connection/session path.
- [x] Refresh has clear reading/failure/current feedback and cannot initiate a hardware mutation.
- [x] DEVICE copy is condensed: verified playback/global gain, active Peak-band count, connection freshness, and validation-only evidence controls where gated; repeated capability paragraphs are removed.
- [x] No new unqualified hardware controls appear.
- [x] Unit/UI tests cover graph/state presentation and Refresh behavior, including stale/read-failure handling. The connected-state Refresh branch is structurally constrained to `scheduleEw300SnapshotRefresh()` and only disconnected/non-connected state invokes `dacSessionRepository.connectEw300()`; no alternate USB/session owner was added.
- [x] Existing EW300/Black Pearl/FiiO shared-session and My DAC regressions remain intact.
- [x] Task closes immediately once focused tests/CI evidence for the changed surface passes or a concrete blocker is recorded.

### Close-out synthesis

- Added deterministic EW300 saved-EQ native matching using the same five-band optimizer and `Ew300Protocol` encode/decode quantization used by Flash; playback/global gain stays outside EQ identity.
- Added `Flat`, exact saved match, ambiguous exact, explicit-lineage-only modified-known, and unknown presentation without fuzzy curve attribution.
- Added the shared `DacEqResponseGraph`, active-band markers, 0 dB reference, and full accessibility description.
- Added compact EQ and DEVICE Refresh. When already connected, `HardwareEqRepository.connectEw300()` performs a read-only snapshot refresh through the existing authoritative session rather than opening/replacing a USB session.
- Condensed DEVICE presentation to playback/global gain, active Peak-band count, connection freshness, and gated validation evidence. No unqualified controls were added.
- Added unit and connected Compose UI regression coverage for exact matching, flat state, shelf-derived Peak identity, graph/accessibility, Refresh callback behavior, and stale/read-failure truthfulness.
- First CI attempt failed only because new test fixtures omitted the required `OpraBand.slope` argument; production sources had compiled. Fixtures were corrected without weakening behavior or validation.
- Exact-head Android CI #1731 on `e505c4107ee9ba491d660d151f8bf8949f851794` passed unit tests, Android lint, debug assembly, release assembly, and connected emulator UI tests.

**Agent/task closed immediately after acceptance.**

## Astra Task 3 — DataVoyager-equivalent shelf corpus reproduction and quality audit

**Status:** OPEN

### Objective

Independently validate low-shelf/high-shelf/mixed-shelf adaptation across representative EQ source paths, reproduce any real remaining defect, and add deterministic golden/regression evidence. Change the optimizer only if the corpus demonstrates a concrete correctness or quality failure; otherwise strengthen tests/documentation without unnecessary algorithm churn.

### Context & Inputs

- Governing product contract: `docs/V0.7_PRODUCT_SUCCESS_CRITERIA.md` and the source-shelf/native-shelf distinction in `docs/EW300_CAPABILITY_MATRIX.md`.
- Shared optimizer/evaluator authority: `domain/kt02h20/Kt02h20FiveBand.kt`, `HardwareEqResponseEvaluator`, and `HardwareEqDeviceSpecs.SIMGOT_EW300`.
- Existing focused tests already demonstrate individual low-shelf and high-shelf adaptation. Those tests are necessary but are not evidence that the broader historical shelf-quality concern is resolved across ingestion/capture paths.
- Representative source families must include OPRA, AutoEQ, community/general presets, strict text import, Personal EQ, and captured DAC EQ where the repository has a deterministic canonical bridge.
- Public source research is allowed for representative fixtures when provenance/license permits; canonical production catalog data must not be edited merely to create tests.
- Exact/Optimized/Not-Suitable labels and response-quality gates are user-facing truth contracts. Never lower validation thresholds just to force a corpus case to pass.

### Out-of-Scope

- No claim of native EW300 low/high-shelf support. The hardware product path remains five Peak bands.
- No hardware mutation, Save, Apply, Flash, Restore, Reset, or new physical test during corpus analysis.
- No canonical source mutation, silent filter substitution outside the established derived-output path, or deletion/weakening of failing validation.
- No broad rewrite of the optimizer unless a reproduced failure demonstrates that the current algorithm is wrong or materially unsafe.
- No publication of third-party EQ data into the app catalog as part of this task.

### Acceptance Criteria

- [ ] Build a representative corpus covering low shelf, high shelf, mixed shelves, shelves plus Peak filters, in-budget and over-budget profiles, and at least one intentionally Not-Suitable case.
- [ ] Exercise canonical profiles through representative OPRA, AutoEQ, community/general, strict-import, Personal EQ, and captured-EQ adapters/bridges where deterministic repository paths exist; document any source path that cannot legitimately express shelves rather than fabricating coverage.
- [ ] For every corpus case, verify canonical source immutability, deterministic repeated output, Peak-only EW300 derived representation, band-count/range constraints, and truthful `Exact`/`Optimized`/`NotSuitable` classification.
- [ ] Measure/verify the existing RMS and maximum response-error gates instead of accepting a representation merely because encoding succeeds.
- [ ] Reproduce the historical shelf concern independently. If a real defect remains, identify root cause and patch minimally with a regression that fails before/fixes after. If no defect remains, record that result and expand regression coverage rather than rewriting the optimizer.
- [ ] Add reusable golden/fixture coverage sufficient to prevent regression in source filter type, frequency, gain, Q/slope interpretation, preamp/headroom handling, determinism, and user-visible suitability classification.
- [ ] Do not weaken existing tests, tolerances, safety limits, or canonical validation to make the corpus green.
- [ ] Run focused optimizer/adapter tests plus exact-head Android CI after any implementation/test changes.
- [ ] Close Task 3 immediately when the corpus and regressions satisfy these criteria; carry any hardware-only uncertainty into the final testing handoff rather than leaving this agent open.

## Pending specialist tasks

- Astra Task 4 — regression/release evidence audit, signed beta candidate, and final testing handoff package.
