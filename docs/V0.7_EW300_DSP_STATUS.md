# v0.7 EW300 DSP status

Updated 2026-09-16. Primary repository: `weekssa/OPRA-EQ-for-UAPP`.

- Feature branch: `feature/v0.7-ew300-dsp`; isolated checkout `/private/tmp/opra-eq-ew300`. Historical v0.6 checkout is not used.
- Phase B: owner has supplied read-only enumeration and standard descriptors for the stock cable. Exact identity evidence now includes VID:PID `31B2:0111`, revision `0101`, manufacturer `LE XIAN`, product `SIMGOT EW300 DSP`, HID interface 3 and a declared 74-byte report descriptor. The first report read returned `-1` because the interface had not been claimed. Exact signed source `1786a0bc4afc8b88bc141f1fd30d44638198e568` then proved the Pixel 9 refuses a non-forced claim (`false`) while Android's driver owns interface 3. A bounded follow-up will briefly isolate only that interface, perform the standard descriptor read, release it, and send no HID report or EQ command. Untouched EQ readback is still pending.
- No EW300 production registry entry, EQ command, filter limits, persistence claim or reset implementation is qualified.
- Merge and v0.7.0 publication remain pending physical PASS and explicit owner authorization.

## Diagnostic launch failure

Owner reported that signed diagnostic `c0083459edbcec095d420b22aa8b65a32ccafa84` installs but immediately stops. Inspection of the downloaded APK (SHA-256 `669b4c7a831c0edd8888dde657a6776742edb8530a3921ffab73aa50c8bf66c4`) confirms no definition of `com.weekssa.opraeqforuapp.diagnostics.Ew300UsbDiscoveryActivity` in any DEX file. The manifest names that missing class as launcher. This is a sufficient launch-failure cause; no phone stack trace was available.

The custom build registered `src/debug/java` as extra Java sources only. AGP 9 built-in Kotlin requires the Kotlin source set. Earlier exception handling around receiver registration could not fix code that was absent from the artifact. The former unit/lint/build/signing passes did not test launchability.

Correction: register the directory as Kotlin sources; require a DEX class-definition check and two actual cold launches of the signed APK on Android 36. The class-definition gate has been tested against the failing owner APK and correctly rejects it. Candidate workflow evidence must pass before another owner handoff. This does not establish any device protocol or physical capability.

The corrected `6ec2ce2` diagnostic passed APK class-definition, pinned signer, alignment, and two Android 36 cold-launch checks before the owner capture. The workflow packages exact source, APK checksum, signer/alignment reports, class-definition report, launch output and rendered UI XML. A failed launch prevents candidate upload. Results attach to the exact workflow run and are not generalized to future builds.

The `1786a0b` signed diagnostic also passed those gates. Owner capture established that Android retained the HID interface and the non-forced claim failed safely. No descriptor transfer followed that refusal, and no cable state was written.
