# v0.7 EW300 DSP status

**Updated:** 2026-09-22
**Recovery branch:** `feature/v0.7-ew300-luna-high` (PR #23; current exact signed candidate recorded below)
**Base:** current `main` at the start of recovery
**Product state:** current source `dea8439956f41cbf83cda314f5f9f53d878c6181` includes session-owned EW300 mutation, non-suspending ViewModel initiation, and exact-baseline post-Save restoration. E033 remains the controlling physical Flash result: 11 writes, one Save, volatile readback, detach, then `ForgottenCoroutineScopeException` before replacement-session verification/final readback/restoration. The current source passed the complete software/security/signing/install/launch gates; no physical result or public support claim is inferred.
**Public release:** not supported by v0.6.0 and not released in v0.7.0

## 2026-09-22 continuation — exact-baseline restoration implemented in software

The current recovery work adds an architecture-aligned post-Save restoration transaction to the
existing EW300 flasher and authoritative DAC-session owner. After a verified Flash, the signed
validation candidate can restore the captured five Peak-band register pairs plus playback/global
gain (`0x66`), send exactly one restoration Save, validate an exact replacement fingerprint and
strictly newer session generation after detach, and require a byte-for-byte final readback before
publishing `restorationVerified=true`. The operation trace now carries baseline fingerprint/session
provenance and an explicit failure/uncertainty reason. The validation-only restore action is absent
from ordinary builds.

Focused software coverage is added for successful exact restoration, caller cancellation during
restoration detach, one Save, wrong/lower replacement generations, wrong identity, final-readback
mismatch, exception-after-Save uncertainty, and truthful unmeasured telemetry. The current PR
head and signed artifact passed the complete remote software, security, signing, installation,
launch, and candidate-publication gates. E033 remains the controlling physical Flash result; the
current candidate is eligible for one bounded physical Flash-plus-restoration session, but no
physical test result is inferred.

## Current exact signed candidate — E035

| Item | Value |
| --- | --- |
| Source commit | `dea8439956f41cbf83cda314f5f9f53d878c6181` |
| APK | `EQ-Library-v0.7.0-beta-dea8439.apk` |
| APK SHA-256 | `72c95bfaedae0f03303c7aac975aa67ca1e7af63b6c40b95809c022238971792` |
| Package/version | `com.weekssa.opraeqforuapp`, `versionCode 7`, `versionName 0.7.0` |
| Signer SHA-256 | `65C1C1256DAE3C49E3548F334C91F0BA991969E9BE9E0B223BA4E253D2114747` |
| Signed workflow | [#1296](https://github.com/weekssa/OPRA-EQ-for-UAPP/actions/runs/35696584387) |
| Artifact digest | `sha256:59b15d4a0a726613452b923cdf6bfda483f43974be1510bdf85f636f80bcb559` |
| Immutable APK | `https://raw.githubusercontent.com/weekssa/OPRA-EQ-for-UAPP/mobile-test-apk/candidates/EQ-Library-v0.7.0-beta-dea8439.apk` |

All applicable software/security/signed-artifact/install/launch gates passed on this same source
SHA. Physical Flash and exact restoration remain open.

## Current decision

The SIMGOT EW300 DSP cable is being added as another DAC through the existing registry, finite-hardware adapter, shared ViewModel-scoped session, and My DAC shell. The same source-neutral canonical EQ pipeline serves OPRA, AutoEQ, community/general sources, imports, Personal EQs, and captured DAC EQs; the target adapter alone decides what the EW300 can represent. Research now qualifies direct-Hz Peak readback and Peak-only capture; shelf snapshots still fail closed.

The frozen signed candidate already records the exact-device persistence qualification as `VERIFIED`.
The continuation promoted only that evidence-backed exact fingerprint and five-band Peak path
into the product implementation. The prior signed candidate `6b3a35c6e8901c89f52ec3588d0feac744cba017`
passed the historical software/signing/install/launch gates but stopped before a verified Flash
result when Android requested permission during the old reconnect race. The current branch adds
the mutation reconnect gate and operation evidence; its exact signed candidate passed the complete
software/release validation cycle, then stopped at an editor global-gain preflight defect with no
write sent. The current branch corrects the EW300 headroom mapping, and signed candidate
`8bb87aac689ce28b1e92e115a3f83f1b59ad1f65` passed the complete replacement software/release
gates, but its pre-Save stop exposed a recurring automatic permission prompt. The current branch
blocks that automatic reconnect until an explicit owner connect action and rechecks that
authorization when a queued automatic callback starts. Signed candidate
`7035518b042a7b19c0495869cf247359ee27b4a2` passed the replacement software/release gates and
is the only candidate with the observed physical edit/restoration session. The bounded follow-up
source `6c41d9f78f7fc34f064309564dd3f7eb9f75562e` closes the reviewed software gaps for read-only
versus mutation reconnect gating, exact replacement-session verification, and unmeasured
telemetry. Its read-only capability report now passes for the exact device; the latest physical
Apply evidence is recorded in E027 and is explicitly `APPLY`, not `FLASH`. E031 now provides the
physical Flash operation report: the operation reached one Save and volatile verification, then
was canceled during the disconnect boundary before replacement-session handling and final readback.
The E032 app-level Compose-scope change was disproved by E033. Source `41aa0b0aca879d8d9b7844e8574a68a5949cc8c8` now moves the
hardware mutation into the authoritative DAC-session owner and makes the Compose callback only
initiate a non-suspending ViewModel event. Durable `Ew300OperationTrace` state carries terminal
success/failure/uncertainty, replacement-session evidence, final-readback truth, and restoration
status. Focused tests cancel the initiating UI caller during simulated detach and require the owned
transaction to continue through exact replacement verification and final readback; separate tests
reject stale generation/fingerprint replacements and keep a failed final readback uncertain. The
existing reconnect gate, permission-before-write ordering, one-Save policy, no write retry, exact
fingerprint gate, and final-readback requirement are unchanged. No physical mutation is authorized
until a signed candidate from this lifecycle recovery passes the complete current-head gates.
See `docs/V0.7_EW300_FULL_BUILD_STATUS.md` for the execution record.

## Evidence already available

The preserved evidence from PR #21 and its discovery history establishes:

- exact observed identity: VID:PID `31B2:0111`, revision `0101`, manufacturer `LE XIAN`, product `SIMGOT EW300 DSP`;
- HID interface 3 and the captured 74-byte report descriptor;
- vendor report IDs `0x4B` and `0x54` with 10-byte input/output payload declarations;
- exact read framing using report `0x4B`, command `0x52`, and register echoes;
- a preserved untouched stock snapshot containing the current slot, five observed band pairs, and a global-gain payload;
- volatile gain/frequency/Q raw-field write, readback, exact restoration, and final full-snapshot matching on the owner’s cable;
- reversible raw filter-code experiments for codes 1–4 with exact restoration; and
- Android diagnostic launchability hardening, including DEX class-definition and cold-launch checks.

These facts qualify the observed raw transport boundaries. They do not, by themselves, qualify acoustic units, full ranges, persistence, Reset, or every retail revision.

## Capability decision table

| Capability | Evidence status | Product decision in this beta |
| --- | --- | --- |
| Exact USB identity | High confidence for the observed identity/revision | Allowlist only the verified VID/PID and exact supported interface behavior |
| Five raw band pairs | Exact transport/readback evidence plus direct-Hz acoustic cross-check | Support guarded readback and complete Peak-only Personal EQ capture |
| Frequency units/range | Same-earpiece DSP-vs-passive acoustic delta fits direct raw-Hz mapping at 0.989 correlation / 0.152 dB RMS; 2× fallback fails the fit | Use direct raw-Hz mapping and bounded codec limits; fail closed outside them |
| Gain units/range | Raw tenths-dB behavior observed; full boundaries not physically established | Use bounded codec limits; no silent clamp |
| Q units/range | Raw thousandths behavior observed; full boundaries not physically established | Use bounded codec limits; no silent clamp |
| Filter types | Raw codes 1–4 reversible; acoustic labels remain provisional | Expose only mappings backed by the final qualification; otherwise reject/omit |
| Disabled/unused bands | Not independently established | Do not invent disabled-band semantics; require complete representable bands |
| Global gain/preamp | `0x66` is corroborated as digital DAC/playback gain; frozen exact-candidate evidence includes playback-gain persistence | Keep it as device state outside captured EQ identity; expose writes only through the exact qualified profile and guarded transaction |
| Canonical capture | Direct-Hz Peak semantics are resolved; shelf acoustics remain unqualified | Allow complete Peak-only snapshots; reject non-Peak codes and exclude playback gain |
| Capability report | Read-only allowlisted batch is implemented and wired into My DAC | Shareable readable/JSON reports; no writes, uploads, or automatic feature unlocks |
| Commit/persistence | Frozen exact signed candidate records Save, Peak, playback-gain, exact restoration, and both power-removal checks as `VERIFIED` | Do not rerun Save qualification; use the frozen evidence and keep mutation limited to the exact qualified fingerprint |
| Reset | Software transaction is implemented with strict baseline/verification guards; new-head physical behavior remains to be checked | Expose only when the app marks the exact profile and operation available; stop on any warning or uncertain result |
| Other DEVICE controls | No exact support established for this EW300 fingerprint | Expose every control established by the exact capability profile; keep unestablished controls absent and unclaimed |

## Remaining hardware gate

No additional discovery loop is planned. Public research and automated analysis resolved the
frequency and playback-gain classification questions but cannot prove nonvolatile Save behavior.
The remaining capability resolution is therefore embedded in the single consolidated signed-beta
session rather than scheduled as a separate test loop. Its two complete power-removal checks prove
temporary persistence and exact baseline restoration before any normal Flash/Reset validation.

The owner is not asked to test until:

1. all software and security gates pass on one exact source commit;
2. the APK package, signer, checksum, and launchability are verified;
3. all unsupported capabilities are explicitly absent from the UI; and
4. one concise product-language checklist is ready.

The previous signed candidate satisfied those software-side prerequisites, but its Apply attempt
was stopped before mutation. The exported report proves zero register writes and zero Save commands.
The next and only planned physical work remains the single consolidated session in
`docs/EW300_DSP_HANDS_ON_CHECKLIST.md`, after the replacement candidate passes the complete gates.

## Current implementation boundary

The full-build continuation contains guarded readback and Peak-only capture for the observed
five-band transport, a versioned exact-fingerprint capability profile, and a shared strict
baseline/restore coordinator for Apply, Flash, and Reset. The optimizer remains source-neutral
across OPRA, AutoEQ, community/general sources, imports, Personal EQs, and DAC captures. Any
volume, headset, UAC, microphone, firmware, bootloader, or other DAC control that the exact
hardware profile genuinely supports must be exposed through the same capability-driven surface.
No such additional support is established for this EW300 fingerprint, so those controls remain
absent and unclaimed in this release; no unrelated Black Pearl controls are inherited.

The normal product UI no longer exposes the historical mutating qualification action. Apply,
persistent Flash, and Reset are guarded by the exact qualified fingerprint, active Direct Flash
setting, current connection, complete baseline, one Save command, and final readback. The signed
validation candidate also exposes the exact post-Save baseline restoration action only when a
verified Flash trace is present; it restores the captured bands and `0x66` with one Save and exact
final readback. An uncertain mutation is terminal and is not retried. No public release claim is
made. No factory-reset claim is made. No Black Pearl control is copied to EW300.

## Evidence provenance

The detailed raw evidence ledger remains in `docs/EW300_DSP_PROTOCOL_NOTES.md`. PR #21 remains open/draft and is preserved as the source history. The separate discovery APK/tooling is not part of the shipping recovery branch; future discovery, if ever needed, belongs in an explicitly non-production diagnostic surface.

## Exit condition

This status can change to hardware-qualified only after the exact candidate records a physical PASS for the applicable EW300 read, edit, Apply, Flash, reconnect, capture, persistence, Reset, and exact restoration behavior, with every result tied to the exact source commit and signed APK. Until a physical PASS is recorded, the release remains NO-GO. The single bounded Flash-plus-exact-restoration session in `docs/EW300_DSP_HANDS_ON_CHECKLIST.md` may be considered only after the exact candidate passes the complete software and signing gates. Merge and release still require explicit owner authorization.
