# v0.7 EW300 DSP status

**Updated:** 2026-09-24
**Branch:** `main` after merged PR #31 ([PR](https://github.com/weekssa/OPRA-EQ-for-UAPP/pull/31)); signed source head `d452004f4f09f026940a7a2da32bae583c3aed43`
**Release:** v0.7.0 remains unreleased and NO-GO until final beta validation, review, and explicit owner approval.

This document is the concise current-state summary. Historical physical and signed-candidate chronology remains authoritative in `docs/EW300_VALIDATION_LEDGER.md`; protocol research remains in `docs/EW300_DSP_PROTOCOL_NOTES.md`; the testable product contract is `docs/V0.7_PRODUCT_SUCCESS_CRITERIA.md`.

## New owner-reported audio-channel investigation

On 2026-09-24 the owner reported that the exact EW300 path plays only through the right earbud and
that the left side crackles at high level, while the same IEMs work with another DAC. This is a
high-priority diagnostic issue, not evidence that the app caused the defect. The durable
investigation and bounded next steps are recorded in
`docs/V0.7_EW300_AUDIO_CHANNEL_INVESTIGATION.md`.

The initial source audit found that EQ Library claims only the EW300 HID PEQ interface and contains
no PCM, channel-routing, stereo/mono, or left/right-balance implementation. The EW300 descriptor
evidence separately identifies UAC stereo playback interfaces. The audit is therefore keeping the
app protocol, Android/UAPP audio route, EW300 UAC/analog path, and cable/earpiece path distinct.
The live installed candidate was previously traced to `main` source `a8abf78275554b44bf2f6a3b0042dfcd8fe8441c`,
matching the owner's installed candidate URL. The corrective source is now merged into `main` at
`d452004f4f09f026940a7a2da32bae583c3aed43`, and its exact signed beta is available below; it is
not yet physically qualified.
No Flash, Reset, Save, Restore, exploratory write, or maximum-volume test is authorized while the
cause is being narrowed. No new public EW300 audio-support claim is made.

The owner then reproduced the same right-only/left-crackle symptom with the EW300 DSP cable on a
computer, while the alternate cable/DAC worked. This strongly excludes an Android-only, UAPP-only,
or runtime EQ Library audio-routing defect. Review of the preserved stock and failed-operation raw
register evidence then identified an app-owned defect: the old EW300 path changed only byte 0 of
stereo digital-gain register `0x66`, leaving byte 1 unchanged. PR #31 now reads the protocol layout,
writes both stereo gain bytes, fails closed for unequal stereo readback, and records stable R8-safe
operation outcomes. No physical audio result or public audio-support claim is made until the owner
confirms corrected readback/audio on the exact signed candidate.

The independent audit in `docs/V0.7_RELEASE_READINESS_AUDIT.md` records the current product scope,
source repairs, and remaining release blockers. The latest validated implementation snapshot before
this status synchronization is `fdc60bb2443aed00c6e58ea8806e4735272e00d9`; Android CI #1776 / run
`35939196029`, CodeQL #1661 / run `35939195977`, catalog currentness #2074 / run
`35939196021`, priority coverage #1559 / run `35939195974`, and dependency submission #2097 / run
`35939191733` all passed on that exact SHA. Android CI includes unit tests, lint, debug/release
assembly, R8 mapping verification, connected emulator tests, and API-26 minified install/cold
launch. The signed-beta workflow is dispatch-only and guards signing to `main`; no exact current
source signed APK or artifact provenance exists. This documentation synchronization creates a later
source SHA; use the live PR for its exact-head gate state and do not carry these results to another
SHA. Do not install or use an earlier candidate.

The 2026-09-23 follow-up repairs are implemented: stale ToneBoosters artifacts no longer revive
after failed conversion; exact old app-owned UAPP exports remain visible under **Needs attention**
when current source compatibility is lost; source-order priority cannot be caller-asserted; the
selected primary source reference is preserved; and newly saved Favorites/General EQs retain their
full canonical profile, selected revision, and source references. The non-destructive Room 8→9
migration leaves older projection-only rows unchanged. These source/library repairs do not change
EW300 protocol behavior or accepted physical evidence; no physical test is needed or requested.
Current source, workflow, and artifact links are recorded below; older APKs are stale for this fix.

## Current automated candidate state

The implementation merged to `main` at `d452004f4f09f026940a7a2da32bae583c3aed43` after PR #31.
Its exact-head automation was green: Android CI #1800 / run `35984178455`, CodeQL #1684 / run
`35982537351`, catalog currentness #2091 / run `35982537329`, priority-community coverage #1576 /
run `35982537311`, and dependency submission #2120 / run `35982580588`. Android CI verified unit
tests, lint, debug/release assembly, R8/minified checks, connected emulator UI, and API-26 minified
install/cold launch. The candidate includes stable non-obfuscated EW300 outcome reporting and
unequal-stereo `0x66` fail-closed guards with targeted no-write/Save tests.

The main-only signed-beta workflow #1356 / run
`https://github.com/weekssa/OPRA-EQ-for-UAPP/actions/runs/36070756903` passed its beta gate,
R8/signature/alignment checks, signed emulator install, cold launch, and immutable publication.
The exact release APK is
`https://raw.githubusercontent.com/weekssa/OPRA-EQ-for-UAPP/mobile-test-apk/candidates/EQ-Library-v0.7.0-beta-d452004.apk`;
SHA-256 `49049fc53b7580ae3026b035cd54c227cee5ea791747448d37fccabb37d234a6`; package
`com.weekssa.opraeqforuapp`; version `0.7.0`/code `7`; release/R8-minified variant; signer
SHA-256 `65C1C1256DAE3C49E3548F334C91F0BA991969E9BE9E0B223BA4E253D2114747`; artifact archive
digest `sha256:b472ce2916c90b82242eb82d8849e4234857b00702d61b12186d9869b19e0b53`.
No physical result or public EW300 support claim is inferred.

## Current product decision

The exact qualified SIMGOT EW300 DSP is implemented as another DAC inside the existing shared My DAC architecture. The shipping capability boundary is deliberately narrower than generic KT02H20-family tooling and narrower than Black Pearl's control inventory.

Supported and implemented for the exact qualified profile:

- exact identity/session recognition and freshness;
- five-band direct-Hz **Peak** EQ readback;
- shared response graph with active-band markers, 0 dB reference, and accessibility description;
- truthful Flat / deterministic exact saved match / ambiguous exact match / Unknown state presentation;
- local five-band Peak editing and Review → Apply;
- guarded Flash from the existing My EQs / EQ Library hardware-output flow;
- Peak-only Personal EQ capture;
- Reset EQ to flat;
- authoritative-session manual Refresh for EQ and DEVICE;
- reconnect/final-readback status and privacy-safe operation evidence;
- read-only playback/global-gain device state; and
- source low/high-shelf adaptation to an **Optimized** five-Peak derived representation only when fixed response-error/safety gates pass.

Playback/global gain is device state outside captured EQ identity. The evidence does not establish a safe independent EW300 volume UX, so no volume slider/write is exposed. DAC filter, gain mode/PGA, amplifier/output topology, balance, microphone gain/control, UAC switching, firmware/update, routing/headset configuration, and factory-device defaults remain **INSUFFICIENT_EVIDENCE**. Firmware/bootloader/erase/calibration/recovery/cross-flash work remains unsafe/out of scope. VID/PID-only or unknown revisions are not authorized.

## Physical qualification is already closed

Do not repeat the qualified physical transaction path merely because later product/docs commits create new SHAs.

- E001: accepted one-time Save/persistence/two-power-cycle/exact-baseline-restoration qualification.
- E037–E040: verified physical Flash, exact-baseline Restore, Apply, and Reset on the source-381 candidate.
- E043–E046: read-only PASS plus verified Flash, exact-baseline Restore, and Reset on exact signed executable source `7599dd52fc9e8c58c96e021f581b86a669dcc148`.
- E047–E048: coherent software/security/signing provenance for the physically tested source and later pre-criteria documentation head.

The runtime matcher now additionally requires the recorded USB device revision. This software
restriction is not itself a new physical qualification; unknown revisions remain read-only.
The final accepted physical evidence still proves only the exact previously authorized unit and
the operations named in E001/E043–E046.

E033 remains historical failure evidence and is **not** the latest physical Flash result. E043–E046 supersede that physical-state conclusion while preserving chronology.

No further Apply, Flash, Restore, Reset, E001 Save qualification, or read-only report is requested. A later signed candidate needs new software/signing provenance, not automatic hardware requalification.

## My DAC UX closure

The previously open EW300 product gaps are closed in the implementation:

- compact shared EQ response graph;
- truthful current/Last-read state and deterministic saved-EQ matching without curve-similarity provenance;
- EQ and DEVICE manual Refresh through the single authoritative session;
- concise DEVICE presentation;
- non-color accessibility semantics and explicit error/stale-state copy; and
- Personal EQ capture wording aligned with actual canonical behavior.

DEVICE deliberately exposes no guessed Black Pearl-style controls. The Black Pearl comparison is a capability checklist only, not protocol authority.

## Shelf-source closure

The historical shelf problem came from the older finite-hardware adapter requiring source filter families to be natively supported. The current representation-v3 path separates source-response compatibility from target-native controls:

1. canonical low/high shelves remain unchanged;
2. the complete source response is evaluated;
3. the EW300 target adapter derives quantized Peak candidates;
4. no more than five Peak bands are selected/refined deterministically;
5. fixed RMS and maximum-error gates decide whether the result is `Optimized` or `Not suitable`; and
6. unsupported filter families fail explicitly rather than being silently dropped.

The regression corpus covers representative OPRA, AutoEQ, community/general, strict import, Personal EQ, captured-DAC, over-budget, determinism, immutability, and Not-Suitable paths. Android CI #1736 passed the corrected corpus together with unit tests, lint, debug/release assembly, and connected emulator tests on the corresponding code plus privacy-safe orchestration documentation. No additional optimizer rewrite was justified by the corpus.

## Capability research closure

Independent research did not promote any new EW300 DEVICE write. Generic KT02H20 sources corroborate `0x66` as digital DAC/playback gain, but they do not establish exact EW300 independent-volume intent, safe range, persistence, or standalone-write semantics. Exact-product material establishes a microphone/media-call cable but not app-managed microphone gain or other DEVICE control semantics. Lack of evidence is not labeled unsupported.

See `docs/EW300_CAPABILITY_MATRIX.md` for the per-control classification.

## Remaining release work

The implementation candidate, exact-head automated gates, and signed-artifact provenance are
complete. Remaining work is one bounded physical audio confirmation and final owner go/no-go:

1. keep maintained release/evidence text synchronized with the current PR and accepted physical evidence;
2. hand the exact signed beta to the owner for clean-install/upgrade, accessibility, offline/error/reconnect, shared-DAC regression, read-only EW300 state, and safe-volume stereo-audio confirmation described in `docs/EW300_DSP_HANDS_ON_CHECKLIST.md`;
3. record the physical result against the exact source/APK; and
4. remain NO-GO for public release/public EW300 hardware-support claims until the owner explicitly approves those actions.

The private qualified-unit serial must not be repeated in public support text or ordinary operation reports. Exact authorization remains enforced by the existing qualification code/evidence boundary.
