# v0.7 EW300 DSP status

**Updated:** 2026-09-22
**Recovery branch:** `feature/v0.7-ew300-luna-high` (PR #23; read the live PR for current HEAD and candidate provenance)
**Release:** v0.7.0 remains unreleased and NO-GO; PR #23 must remain draft until explicit approval.

## Current closeout decision

Physical Apply, Flash, exact-baseline post-Save Restore, and Reset were verified on source 381 in E037-E040. Newer owner reports E043-E046 add exact-candidate read-only, Flash, Restore, and Reset evidence on signed source `7599dd52fc9e8c58c96e021f581b86a669dcc148`, the exact PR #23 candidate at the time of testing.

On source 7599, capability report JSON (17) passed for the exact fingerprint. Operation JSON (11) Flash, JSON (12) Restore, and JSON (13) Reset all show one Save, zero permission requests before first write, 11 writes, exact replacement identity, final readback, and known state. Restore has `restorationVerified=true`. Permission request count is one total per operation; this does not violate the zero-before-first-write guard. Replay and competing-connection-job telemetry remain null/unmeasured.

The Restore report proves the captured baseline was restored when that operation completed. The separately reported later Reset completed successfully; do not infer that the post-reset state still equals an arbitrary pre-Flash baseline. The read-only snapshot confirms known state, but is not a byte-for-byte comparison to the original baseline.

Candidate 7599 passed Android CI #1712, CodeQL #1597, catalog currentness #2010, priority coverage #1495, dependency submission #2017, and signed candidate #1316 on one exact SHA. This documentation-only closeout creates a new head and requires fresh exact-head gates/provenance; PR #23 is authoritative for that new manifest. Because this update does not change runtime behavior, no physical mutation retest is justified.

No further Apply, Flash, Restore, Reset, or E001 Save qualification is needed. The remaining possible owner-facing check is non-mutating Personal EQ capture/value/provenance confirmation; verify the My EQs Flash-review path only if the successful Flash entrypoint was not already My EQs. The operation report does not identify the entry surface. Capture UX remains “not yet evidenced,” not unsupported hardware.

Merge, publication of v0.7.0, and any public EW300 support claim require explicit owner approval.

The dated candidate sections and implementation history that follow are retained as chronology; their older “current” statements are superseded by this active closeout and the live PR #23 manifest.


## Historical signed candidate — `02f2697e20dda0c33f72973cb140a3be5d592cef`

At that point in the chronology, this candidate was a public My DAC UX and validation-control follow-up after the source-381 physical operation set. It kept the qualified EW300 transaction implementation unchanged while aligning the product surface with the shared DAC flow and covering removal of validation-only controls from public builds.

| Item | Value |
| --- | --- |
| Source commit | `02f2697e20dda0c33f72973cb140a3be5d592cef` |
| APK | `EQ-Library-v0.7.0-beta-02f2697.apk` |
| APK SHA-256 | `39737d9a2aeddc6c2db1e4724c3875286bf2c2236cf858f88f5b98e854ed8314` |
| Package/version | `com.weekssa.opraeqforuapp`, `versionCode 7`, `versionName 0.7.0` |
| Signer SHA-256 | `65C1C1256DAE3C49E3548F334C91F0BA991969E9BE9E0B223BA4E253D2114747` |
| Signed workflow | [#1314](https://github.com/weekssa/OPRA-EQ-for-UAPP/actions/runs/35761742008) |
| Signed artifact digest | `sha256:0b2460e79d09d915c770f6d609e80706772ed86c0dea3856a36c4188da7e623e` |
| Immutable APK | `https://raw.githubusercontent.com/weekssa/OPRA-EQ-for-UAPP/mobile-test-apk/candidates/EQ-Library-v0.7.0-beta-02f2697.apk` |

Android CI #1710, CodeQL #1595, catalog currentness #2008, priority-community coverage #1493, dependency submission #2015, and Signed EQ Library Beta Candidate #1314 passed on this exact source. The signed workflow also passed alignment, pinned-signer
verification, installation, cold launch, and immutable candidate publication. The candidate is
ready for one bounded owner session to verify the final current-head Flash UI availability and
validation evidence path; this is not Save requalification or a request for broader protocol
research. Existing physical E037-E040 remain tied to source 381 and are not silently attributed to
this UI-only follow-up.

## 2026-09-22 continuation — exact-baseline restoration implemented in software

The current recovery work adds an architecture-aligned post-Save restoration transaction to the
existing EW300 flasher and authoritative DAC-session owner. After a verified Flash, the signed
validation candidate can restore the captured five Peak-band register pairs plus playback/global
gain (`0x66`), send exactly one restoration Save, validate an exact replacement fingerprint and
strictly newer session generation after detach, and require a byte-for-byte final readback before
publishing `restorationVerified=true`. The operation trace now carries baseline fingerprint/session
provenance and an explicit failure/uncertainty reason. The validation-only restore action is absent
from ordinary builds.

Focused software coverage is added for successful exact restoration, caller cancellation during
restoration detach, one Save, wrong/lower replacement generations, wrong identity, final-readback
mismatch, exception-after-Save uncertainty, and truthful unmeasured telemetry. The latest verified signed head before this documentation-only synchronization passed the complete remote software, security, signing, installation, launch, and candidate-publication gates. Physical E037-E040 on source 381 already close the bounded operation set; this documentation-only synchronization does not create new physical evidence. No further Flash, Restore, Apply, Reset, or Save qualification is requested.

The same candidate adds an evidence-safe Device tab for EW300. It exposes only read-only
playback/global-gain state, active Peak-band count, and current-versus-last-read connection
freshness with operation-report/reconnect status. It does not copy Black Pearl controls or claim
unsupported EW300 hardware functions. Source low- and high-shelf profiles can now be converted by
a deterministic full-response fit to the qualified five-band Peak representation; native EW300
readback, capture, editor, Flash, and Reset remain Peak-only.

## 2026-09-22 public v0.7 UX boundary

The live product surface follows the approved shared My DAC flow rather than exposing the EW300
validation harness. My DAC → EQ is an active product surface: it shows the current or last-read
hardware EQ, response graph, local five-band Peak editing, Review → Apply, Peak-only Personal EQ
capture, Reset EQ to flat, and verified reconnect/readback feedback. Flash remains available from
My EQs and EQ Library through the existing guarded flow.

My DAC → DEVICE shows the exact profile's reported playback/global-gain state, active Peak-band
count, and current-versus-last-read connection freshness. Global gain participates in guarded EQ
transactions and exact restoration, but it is not exposed as an independent volume control because
that standalone control is not separately qualified for this fingerprint. Unsupported volume,
headset, UAC, microphone, firmware, bootloader, and unrelated DAC controls remain absent.

The signed beta validation candidate may expose the read-only capability report, readable/JSON
evidence sharing, operation-report sharing, and exact-baseline restoration action needed for the
bounded owner session. Those controls are validation-only and are excluded from the public release
build. The one-time Save qualification is internal evidence and is not a product action.

## 2026-09-22 physical evidence update

The owner supplied paired readable/JSON operation reports from the exact signed source `381286eb7ea29b0707e6da75f3565bada1d73928`:
- `APPLY`: operation `9308009d-e9f0-4e9a-bf09-1aac01ab00a6`, terminal `VERIFIED`, 10 writes, one Save, replacement identity matched, final readback matched.
- `RESET`: operation `233bd9f6-5654-456b-8006-2846a5dd90ae`, terminal `VERIFIED`/Success, 11 writes, one Save, replacement identity matched, final readback matched.
- Earlier same-source `FLASH` and exact-baseline `RESTORE` reports are recorded as E037-E038.

The reports keep `mutationReplayCount` and `competingConnectionJobCount` unmeasured/null. The Reset report proves the Reset transaction and its final target readback, but does not export raw before/after register values; therefore unchanged playback/global-gain values are not independently claimed from that report alone.

## Historical exact signed implementation candidate — E036

| Item | Value |
| --- | --- |
| Source commit | `381286eb7ea29b0707e6da75f3565bada1d73928` |
| APK | `EQ-Library-v0.7.0-beta-381286e.apk` |
| APK SHA-256 | `12ae91cb9f9eed88bba45c17f56322c526133ce5de61bbd68d108eacbb84bb9f` |
| Package/version | `com.weekssa.opraeqforuapp`, `versionCode 7`, `versionName 0.7.0` |
| Signer SHA-256 | `65C1C1256DAE3C49E3548F334C91F0BA991969E9BE9E0B223BA4E253D2114747` |
| Signed workflow | [#1303](https://github.com/weekssa/OPRA-EQ-for-UAPP/actions/runs/35704011756) |
| Artifact digest | `sha256:4672b6c03153f56d00a3aceab1f49a4d8f83f4e78acb5584a9fbee35087789f1` |
| Immutable APK | `https://raw.githubusercontent.com/weekssa/OPRA-EQ-for-UAPP/mobile-test-apk/candidates/EQ-Library-v0.7.0-beta-381286e.apk` |

All applicable software/security/signed-artifact/install/launch gates passed on this same source
SHA. Physical Flash, exact restoration, Apply, and Reset are verified on source 381 (E037-E040); the latest signed documentation head before this synchronization is 2f99. This documentation-only change requires fresh gates. Personal EQ capture remains unevidenced by these reports.

## Current decision

The SIMGOT EW300 DSP cable is being added as another DAC through the existing registry, finite-hardware adapter, shared ViewModel-scoped session, and My DAC shell. The same source-neutral canonical EQ pipeline serves OPRA, AutoEQ, community/general sources, imports, Personal EQs, and captured DAC EQs; the target adapter alone decides what the EW300 can represent. Direct-Hz Peak readback and Peak-only native capture are qualified; source low/high shelves are now handled by an explicit full-response Peak-only optimization, while native EW300 shelf readback/capture/editor support remains unestablished.

The frozen signed candidate already records the exact-device persistence qualification as `VERIFIED`.
The continuation promoted only that evidence-backed exact fingerprint and five-band Peak path
into the product implementation. The prior signed candidate `6b3a35c6e8901c89f52ec3588d0feac744cba017`
passed the historical software/signing/install/launch gates but stopped before a verified Flash
result when Android requested permission during the old reconnect race. The current branch adds
the mutation reconnect gate and operation evidence; its exact signed candidate passed the complete
software/release validation cycle, then stopped at an editor global-gain preflight defect with no
write sent. The current branch corrects the EW300 headroom mapping, and signed candidate
`8bb87aac689ce28b1e92e115a3f83f1b59ad1f65` passed the complete replacement software/release
gates, but its pre-Save stop exposed a recurring automatic permission prompt. The current branch
blocks that automatic reconnect until an explicit owner connect action and rechecks that
authorization when a queued automatic callback starts. Signed candidate
`7035518b042a7b19c0495869cf247359ee27b4a2` passed the replacement software/release gates and
is the only candidate with the observed physical edit/restoration session. The bounded follow-up
source `6c41d9f78f7fc34f064309564dd3f7eb9f75562e` closes the reviewed software gaps for read-only
versus mutation reconnect gating, exact replacement-session verification, and unmeasured
telemetry. Its read-only capability report now passes for the exact device; the latest physical
Apply evidence is recorded in E027 and is explicitly `APPLY`, not `FLASH`. E031 now provides the
physical Flash operation report: the operation reached one Save and volatile verification, then
was canceled during the disconnect boundary before replacement-session handling and final readback.
The E032 app-level Compose-scope change was disproved by E033. Source `41aa0b0aca879d8d9b7844e8574a68a5949cc8c8` now moves the
hardware mutation into the authoritative DAC-session owner and makes the Compose callback only
initiate a non-suspending ViewModel event. Durable `Ew300OperationTrace` state carries terminal
success/failure/uncertainty, replacement-session evidence, final-readback truth, and restoration
status. Focused tests cancel the initiating UI caller during simulated detach and require the owned
transaction to continue through exact replacement verification and final readback; separate tests
reject stale generation/fingerprint replacements and keep a failed final readback uncertain. The
existing reconnect gate, permission-before-write ordering, one-Save policy, no write retry, exact
fingerprint gate, and final-readback requirement are unchanged. No physical mutation is authorized
until a signed candidate from this lifecycle recovery passes the complete current-head gates.
See `docs/V0.7_EW300_FULL_BUILD_STATUS.md` for the execution record.

## Evidence already available

The preserved evidence from PR #21 and its discovery history establishes:

- exact observed identity: VID:PID `31B2:0111`, revision `0101`, manufacturer `LE XIAN`, product `SIMGOT EW300 DSP`;
- HID interface 3 and the captured 74-byte report descriptor;
- vendor report IDs `0x4B` and `0x54` with 10-byte input/output payload declarations;
- exact read framing using report `0x4B`, command `0x52`, and register echoes;
- a preserved untouched stock snapshot containing the current slot, five observed band pairs, and a global-gain payload;
- volatile gain/frequency/Q raw-field write, readback, exact restoration, and final full-snapshot matching on the owner’s cable;
- reversible raw filter-code experiments for codes 1–4 with exact restoration; and
- Android diagnostic launchability hardening, including DEX class-definition and cold-launch checks.

These facts qualify the observed raw transport boundaries. They do not, by themselves, qualify acoustic units, full ranges, persistence, Reset, or every retail revision.

## Capability decision table

| Capability | Evidence status | Product decision in this beta |
| --- | --- | --- |
| Exact USB identity | High confidence for the observed identity/revision | Allowlist only the verified VID/PID and exact supported interface behavior |
| Five raw band pairs | Exact transport/readback evidence plus direct-Hz acoustic cross-check | Support guarded readback and complete Peak-only Personal EQ capture |
| Frequency units/range | Same-earpiece DSP-vs-passive acoustic delta fits direct raw-Hz mapping at 0.989 correlation / 0.152 dB RMS; 2× fallback fails the fit | Use direct raw-Hz mapping and bounded codec limits; fail closed outside them |
| Gain units/range | Raw tenths-dB behavior observed; full boundaries not physically established | Use bounded codec limits; no silent clamp |
| Q units/range | Raw thousandths behavior observed; full boundaries not physically established | Use bounded codec limits; no silent clamp |
| Filter types | Raw codes 1–4 reversible; acoustic labels remain provisional | Expose only mappings backed by the final qualification; otherwise reject/omit |
| Disabled/unused bands | Not independently established | Do not invent disabled-band semantics; require complete representable bands |
| Global gain/preamp | `0x66` is corroborated as digital DAC/playback gain; frozen exact-candidate evidence includes playback-gain persistence | Keep it as device state outside captured EQ identity; expose writes only through the exact qualified profile and guarded transaction |
| Canonical capture | Direct-Hz Peak semantics are resolved; native shelf semantics remain unqualified | Allow complete Peak-only snapshots; reject non-Peak native codes and exclude playback gain |
| Source shelf adaptation | Full-response low/high-shelf fitting to EW300 Peak bands is implemented and bounded by the existing RMS/max-error gates | Allow `Optimized` Peak-only output for source low/high shelves; do not describe this as native shelf hardware support |
| Capability report | Read-only allowlisted batch is implemented and wired into My DAC | Shareable readable/JSON reports; no writes, uploads, or automatic feature unlocks |
| Commit/persistence | Frozen exact signed candidate records Save, Peak, playback-gain, exact restoration, and both power-removal checks as `VERIFIED` | Do not rerun Save qualification; use the frozen evidence and keep mutation limited to the exact qualified fingerprint |
| Reset | Software transaction is implemented with strict baseline/verification guards; physical Reset transaction verified on source 381; raw before/after gain values are not included in the operation report | Expose only when the app marks the exact profile and operation available; stop on any warning or uncertain result |
| Other DEVICE controls | No exact support established for this EW300 fingerprint | Expose every control established by the exact capability profile; keep unestablished controls absent and unclaimed |

## Remaining owner evidence gate

After the new documentation-reconciliation source passes the complete exact-head software, security, signing, install, and launch gates, perform only the non-mutating owner check in `docs/EW300_DSP_HANDS_ON_CHECKLIST.md`: exact-device read-only report, one Personal EQ capture with five-band values/provenance review, and open/cancel the My EQs Flash review before final write confirmation. Do not repeat Apply, Flash, Reset, Restore, or E001 Save qualification. If the report or capture is uncertain, stop without mutation.

## Current implementation boundary

The full-build continuation contains guarded readback and Peak-only capture for the observed
five-band transport, a versioned exact-fingerprint capability profile, and a shared strict
baseline/restore coordinator for Apply, Flash, and Reset. The optimizer remains source-neutral
across OPRA, AutoEQ, community/general sources, imports, Personal EQs, and DAC captures. Any
volume, headset, UAC, microphone, firmware, bootloader, or other DAC control that the exact
hardware profile genuinely supports must be exposed through the same capability-driven surface.
No such additional support is established for this EW300 fingerprint, so those controls remain
absent and unclaimed in this release; no unrelated Black Pearl controls are inherited.

The public product UI no longer exposes the historical mutating qualification action or the
validation report/share controls. My DAC → EQ is the usable five-band Peak path: readback, local
editing, Review → Apply, Peak-only capture, Reset EQ to flat, and verified reconnect/final-readback
feedback. My EQs and EQ Library remain the Flash surfaces.

My DAC → DEVICE reports only the exact profile's verified playback/global-gain state, active
Peak-band count, and current/last-read connection freshness. It does not inherit Black Pearl
controls or claim unsupported EW300 functions. Apply, persistent Flash, and Reset remain guarded
by the exact qualified fingerprint, active Direct Flash setting, current connection, complete
baseline, one Save command, and final readback. The signed validation candidate alone exposes
capability/operation report sharing and exact post-Save baseline restoration; these controls are
not part of the public release. An uncertain mutation is terminal and is not retried. No factory
reset claim is made. No Black Pearl control is copied to EW300.

## Evidence provenance

The detailed raw evidence ledger remains in `docs/EW300_DSP_PROTOCOL_NOTES.md`. PR #21 remains open/draft and is preserved as the source history. The separate discovery APK/tooling is not part of the shipping recovery branch; future discovery, if ever needed, belongs in an explicitly non-production diagnostic surface.

## Exit condition

The physical mutation gate is already closed by E037-E040 for the exact qualified transaction behavior on source 381. The remaining product-evidence gate is a successful non-mutating Personal EQ capture and My EQs Flash-review availability check on the latest exact signed candidate after its applicable gates pass. Record that result without attributing a new Flash/Restore/Apply/Reset run to the candidate. Release remains NO-GO until capability scope, final review, synchronized release records, and explicit owner approval are complete.
