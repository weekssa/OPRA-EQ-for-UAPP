# v0.7 EW300 full-build status

**Updated:** 2026-09-21
**Implementation branch:** `feature/v0.7-ew300-luna-high` (PR #23 head)
**Authoritative frozen evidence commit:** `847eaba8dfa14b8e63ab9ed2326f1e3b0f07814f`  
**Final software/signed candidate commit:** `f8707788531cfdef33cd46d79c5c53e649480232`
**Preserved evidence branch:** `v0.7-ew300-candidate`  
**PR:** [#23](https://github.com/weekssa/OPRA-EQ-for-UAPP/pull/23), must remain draft

This document records the full-build continuation after the frozen signed-candidate
qualification. It does not replace the raw evidence ledger in
`docs/EW300_DSP_PROTOCOL_NOTES.md`, and it does not authorize a merge, release, or public
EW300 support claim.

## Frozen evidence accepted

The exact signed candidate at `847eaba8dfa14b8e63ab9ed2326f1e3b0f07814f` produced a read-only
PASS for this exact device fingerprint:

```text
vid=31b2|pid=111|manufacturer=LE XIAN|product=SIMGOT EW300 DSP|serial=2024-07-03-0000-0000-0000|interface=3
```

The signed candidate also recorded `VERIFIED` persistence: EW300 Save, Peak persistence,
playback-gain persistence, exact baseline restoration, and both complete power-removal checks
passed. The artifact evidence is retained as frozen history and the Save qualification is not
run again.

Frozen APK evidence:

| Item | Value |
| --- | --- |
| APK | `EQ-Library-v0.7.0-beta-847eaba.apk` |
| Package | `com.weekssa.opraeqforuapp` |
| Version | `versionCode 7`, `versionName 0.7.0` |
| APK SHA-256 | `51392e8b79b4b7ffe07b3298a4142577f3df7490de56cd146baa8094aa9d6a54` |
| Signer SHA-256 | `65C1C1256DAE3C49E3548F334C91F0BA991969E9BE9E0B223BA4E253D2114747` |
| Signature schemes | v2 and v3 |

## Final signed candidate

The existing signed-beta workflow passed on the final software head. The immutable APK was
published to the temporary `mobile-test-apk` testing surface; this is not a release publication.

| Item | Value |
| --- | --- |
| Source commit embedded in APK | `f8707788531cfdef33cd46d79c5c53e649480232` |
| Artifact filename | `EQ-Library-v0.7.0-beta-f870778.apk` |
| APK SHA-256 | `b512a949c22848ad991b38831c082c59d9ea223ed3099108c39e1aaec353f033` |
| Package ID | `com.weekssa.opraeqforuapp` |
| Version | `versionCode 7`, `versionName 0.7.0` |
| Signer SHA-256 | `65C1C1256DAE3C49E3548F334C91F0BA991969E9BE9E0B223BA4E253D2114747` |
| Signature schemes | v2 and v3; verified by `apksigner` in signed workflow #1256 |
| Alignment | Passed `zipalign` in signed workflow #1256 |
| Installation | Passed signed APK installation on the Android emulator in workflow #1256 |
| Cold launch | Passed explicit `MainActivity` cold launch and version checks in workflow #1256 |
| Workflow | [Signed EQ Library Beta Candidate #1256](https://github.com/weekssa/OPRA-EQ-for-UAPP/actions/runs/35571297392) |
| Immutable APK URL | `https://raw.githubusercontent.com/weekssa/OPRA-EQ-for-UAPP/mobile-test-apk/candidates/EQ-Library-v0.7.0-beta-f870778.apk` |

The signed workflow was invoked with `-PCANDIDATE_SOURCE_SHA` set to the exact full commit.
Ordinary/debug builds do not expose the
Save qualification action; that historical qualification remains disabled in the product UI.

The corrected candidate also makes the shared editor target label device-specific. EW300 now
renders `SIMGOT EW300 DSP · Slot 0`; the earlier hard-coded `TRN Black Pearl · Slot 0` label was
removed and covered by an emulator UI regression test. No hardware mutation was attempted with
the mislabeled candidate.

## Follow-up workflow fix

During the owner walkthrough, the EW300 editor correctly showed `SIMGOT EW300 DSP · Slot 0`,
but the Review changes screen contained only the local safety-gain change (`+0.00 → -2.00 dB`)
and no Peak-band change. That was a local edit preview, not a failed hardware operation. The
owner was correctly told not to press **Apply to DAC**.

The follow-up fix in `f8707788531cfdef33cd46d79c5c53e649480232`:

- removes stale DEVICE-tab wording that incorrectly said Flash and Reset were still pending;
- explains that only the exact EW300 Peak path is available and that the completed Save
  qualification must not be run again;
- enables the managed My EQs Flash entry for EW300 only when Direct Flash is enabled, EW300 is
  the active output, and the exact EW300 session is connected; and
- preserves the existing exact-profile optimizer, no-guess behavior when multiple DACs are
  connected, and fail-closed rejection of unsupported profiles.

Focused tests cover the EW300 Flash gate, ambiguous multi-DAC selection, and the updated
DEVICE-tab safety wording. The replacement signed APK is the artifact recorded above. The owner
should not apply the old gain-only preview and should use the replacement APK for the next guided
session.

## Full-build implementation completed in this continuation

- Added a versioned EW300 capability profile with exact manufacturer, product, interface, and
  qualified serial fingerprint checks. VID/PID alone cannot authorize a mutation.
- Promoted only the evidence-backed five-band Peak path to the qualified product capability
  catalog. Playback gain remains device state and is not part of a captured EQ profile.
- Removed the Save qualification action from the normal product UI. The historical qualification
  plumbing remains only for evidence/report compatibility and tests.
- Added the shared strict baseline and volatile-restoration coordinator used by EW300 mutation
  paths. It requires four-byte reads for the complete read-only register set, never retries a
  write, and never commits a restoration.
- Changed EW300 editor Apply to use the persistent Save command once, then require fresh final
  band and gain readback.
- Enabled the exact-profile EW300 Flash and Reset product paths behind the existing Direct Flash,
  active-target, connection, and exact-fingerprint gates.
- Updated user-facing wording so the app distinguishes the verified EW300 Peak path from
  unsupported Black Pearl controls and from unsupported firmware-level operations.
- Added unit coverage for exact profile authorization and updated flasher tests for the shared
  strict transaction boundary.

## Software validation result

The final head passed one coherent release validation cycle through the repository’s GitHub Actions
workflows. The local checkout has no Gradle wrapper or system Gradle executable. The final gates
passed were:

1. Android unit tests, lint, debug assembly, release assembly, and connected emulator Compose UI.
2. EW300 protocol, capability-profile, optimizer, readback, Apply, Flash, Reset, reconnect, and
   failure-path tests, including no automatic retry after an uncertain mutation.
3. Compose UI/emulator validation for the exact-profile UI and the absence of the Save
   qualification action from ordinary product builds.
4. Black Pearl and FiiO regression tests, catalog-currentness, priority-community coverage,
   dependency submission, CodeQL, and patch-integrity checks.
5. A signed beta built from one exact source SHA, with the repository-pinned signer, package and
   version metadata, APK digest, signature schemes, alignment, source-SHA display, installation,
   and cold-launch checks recorded.

Final PR checks: Android CI #1644, CodeQL #1526, catalog currentness #1940, priority-community
coverage #1425, dependency submission #1931, all successful for `f8707788…`. PR #23 remains open
and draft.

All software, security, signing, installation, and launch gates now pass. Physical testing may now
be requested as one consolidated owner session.

## Physical validation boundary after software gates

There will be one consolidated owner session using the exact signed APK. The session will use only
the app’s labeled controls and will cover read-only preflight, exact-device identity, Peak-only
Apply, readback/capture, Flash if enabled, full power removal/reconnect, Reset only if enabled,
reconnect recovery, and the retained Black Pearl regression checks. The already completed Save
qualification is evidence and is not repeated.

The owner must stop and export both readable and JSON reports on an unexpected identity, warning,
crash, failed restoration, outside-instruction disconnect, or `UNCERTAIN`. An uncertain mutation
is diagnosed before any retry; the app never retries it automatically.

## USB permission-flow follow-up

The first owner attempt to use the newly enabled EW300 Flash path did not produce a verified Flash
result. Android showed both the normal USB permission prompt and a system app chooser for `USB
Audio Player PRO` versus `EQ Library` while the EW300 was reconnecting. The owner was told to stop
and not retry the mutation. This is not hardware PASS evidence and is not counted as a Flash result.

The app previously advertised a manifest-wide `USB_DEVICE_ATTACHED` handler in addition to its
exact in-app USB session. That declaration can make Android treat EQ Library as a competing USB
handler and relaunch it during a brief EW300 re-enumeration. The follow-up removes only that
automatic system-handler declaration. The app still uses its exact manufacturer/product/HID
identity checks, the in-app USB permission request, and the existing dynamic attach/detach
reconnect handling. No device authorization, protocol command, or retry policy is broadened.

The replacement candidate must pass the complete software, security, signing, installation, and
cold-launch gates again before the owner is asked to test Flash. Until then, the owner must not
retry Flash or Save qualification.

## Intentionally unsupported

This build does not authorize volume, headset, UAC, microphone, other Black Pearl controls,
firmware, bootloader, erase, calibration, recovery, cross-flash, unknown EW300 revisions, or
compatibility claims based only on a chip family or VID/PID. “Fully functional” means only the
exact EW300 Peak EQ feature set verified for the qualified fingerprint and signed candidate.

## Approval boundary

After software gates and the consolidated hardware session pass, update PR #23 and the maintained
documentation with the signed artifact and hardware reports. Keep PR #23 in draft. Merging,
publishing a release, and making a public EW300 support claim require separate explicit approval.
