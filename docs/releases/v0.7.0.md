# v0.7.0 — draft, not released

Planned: SIMGOT EW300 DSP USB-C cable support through the existing hardware registry and approved My DAC flows. Evidence collection is still in progress; no hardware support or limits are claimed yet.

Development tooling: a separate signed read-only USB diagnostic now requires packaged launcher validation and two Android 36 cold launches. The earlier signed diagnostic launch failure was traced to omitted Kotlin sources in its custom build type.

Publication and merge require the completed protocol, regression and signed-candidate gates followed by the owner's exact-candidate physical PASS and explicit authorization.
